# L-Mechrevo 更新日志

格式约定：每个版本一个小节，标题为 `## <版本号> — <日期>`；变更按「新增功能 / 修复 / 界面与体验 / 安全加固 / 内部」分组，每条一行，写用户可感知的行为变化与根因。最新版本在最上面，新条目加在文件顶部。

## 0.289.0-beta14 — 2026-09-14

### 新增功能

- 「更多开关」新增「深度睡眠」：走 GCU 动作修改"重启后生效"的关机/睡眠策略，带 pending 状态与重启提示。
- 「更多开关」新增「开机自启动」：实现为用户级计划任务（非注册表 Run 键），无需管理员权限；开关状态读取真实系统状态，写入失败会自动回滚。
- 「仅关闭显示器」重做为「息屏（不睡眠）」：亮度归零并阻止系统空闲休眠，动鼠标/键盘即恢复，另有 30 分钟看门狗兜底；亮度读取失败时不执行，避免盲操作。
- 屏幕校色改为「屏幕」行头的圆角下拉（仅 默认 / sRGB），删除 P3 / AdobeRGB 选项与整个二级弹窗。
- 「静音狂暴」入口按机型支持情况门控：不支持或未知时不显示入口。

### 修复

- 灯光睡眠后键盘灯不亮：根因是键盘电源"未确认"被当成致命错误，导致 `StartMode` 被跳过；现在电源回读未确认不再阻止本地 HID 效果恢复。
- 灯光睡眠后灯带/Logo 不恢复：根因是效果重放被"电源回读"卡住、令牌只在确认时记录导致无重试；现在外置通道确认失败时按通道有界重试，直到确认。
- 键盘/灯带/Logo 熄灭与恢复不同步：根因是两条技术路径各自为政、各被独立的 GCU 确认门卡住；改为"单周期、先下发、确认不参与关键路径"，实测恢复对齐差值 9ms（旧实现差 3.65–5.46 秒）。
- 按 Fn 调键盘亮度时闪回厂商灯效：检测到固件接管后立即重申自定义帧模式（配合代际守卫，单次重申），不再等待 180ms。
- 自定义模式切换时先闪「切换未确认」再变「已激活」：确认窗口过短所致；改为显示「切换中…」并加宽限轮询。
- GPU 超频首次打开状态不同步：按钮显示关、滑条却可以拉。
- 关闭 GPU 超频后数值被复位为 0：现在关闭时保留用户设置的数值。
- 设置弹窗出现多余的空分组标题：分组内没有任何可见行时，标题与行一起隐藏。
- 二级界面先闪到屏幕左上角再跳回主窗旁：改为首次绘制之前就确定尺寸与位置。
- 「仅关闭显示器」导致整机进入 Modern Standby 休眠（S0 机型上"关屏"即待机）：已随「息屏（不睡眠）」重做修复。
- 唤醒后控制台卡死：根因是在 UI 线程上同步执行阻塞式 `SendMessage` 广播（`SC_MONITORPOWER`），广播期间系统进入待机会把 UI 线程挂住；已移除该广播路径。

### 安全加固

- 破坏性操作加固：4 处 `shutdown /r` 重启与「开机自启动」「深度睡眠」的系统写入，改为只接受真实点击 + 500ms 内的新鲜真实输入；回显/程序化赋值（含 UI 自动化）一律不触发；重启改在后台线程执行。
- 测试接缝收口：把绕过测试接缝的旧自启动 API 改为统一走接缝，避免测试触碰真实计划任务。

### 内部

- 删除 7 个零引用文件（Keystone / Audio / ColorUtilities / IconHelper / Slider / RTrackBar / NumericUpDownWithUnit）并清理残留引用；移除 NAudio.Wasapi 依赖。
- UI 审计新增防回归检测：设置弹窗与更新窗纳入审计范围；新增「停靠根溢出」「绝对列溢出」检测。
- 无障碍名称与界面文字对齐（狂暴/静音狂暴按钮）。
- 新增/扩充测试：灯光对齐、键盘恢复、破坏性操作守卫、GPU 超频状态、二级界面首帧、空分组标题、设置分组等；全量测试 1483 通过 / 0 失败 / 1 跳过。

---

## beta13（2026-08-30）

- 修复部分 40/50 系机型显卡偏移超过 245MHz 后被错误判定为未应用；在驱动范围不足且 GCU 明确支持时改走 GCU 并等待状态回读。
- 修复 GCU 或水冷设备启动较慢时显卡模式和水冷自动连接只尝试一次的问题。

这一版撇开界面，只审底层：MQTT 连接生命周期、状态解析与发布的一致性、并发与资源管理、以及同一概念在代码里的对应关系。

真机验证从 47 通过 / 2 失败变成 **48 通过 / 1 失败**，剩下那一个已确认不是我们的代码问题。有两处修复在日志里前后对照得很清楚，下面单列。

**同一层映射写了两份，其中一份是错的**

这是本轮最值得记的一处，因为它的症状会让用户觉得「开关点了跳回去」，而代码看起来完全正常。

灯带主题到状态键的映射在 `MechrevoService` 里存在两份。`SupportsLightTopic` 正确区分四条灯带；而 `SetLightPower` 的确认逻辑自己写了一个三元表达式，只区分 Logo，把 `Hinge` 与 `Sync` 都算成了 `"lightbar"`。于是开关铰链灯带时，确认看的是**主灯带**的状态——命令其实生效了，确认却失败，然后白跑三轮 GETSTATUS 重试，界面最后按「失败」回滚勾选。

真机日志的前后对照：

```
修复前  SetLightPower(HidLightbar_Hinge/Ctrl, True) not confirmed
修复后  SetLightPower(HidLightbar_Hinge/Ctrl, True) confirmed
```

现在两处共用同一个 `LightTopicToQuickSwitchKey`。判定顺序有个隐含依赖也写进注释了：三条子灯带的主题都以 `HidLightbar_` 开头，所以必须先判子灯带、最后才能落到主灯带。

**模式切换的过期包过滤窗口在失败路径上不解除**

切模式前会武装一个 8 秒窗口，用来丢弃 GCU 排队中的切换前旧状态包。`MechrevoHw.SetMode` 在发布失败时会解除武装，但服务层的 `SwitchMode` / `SwitchCustomProfile` 两条路都没有——`catch` 只记了日志，`finally` 只放锁。

后果是断线时切一次模式，接下来 8 秒内**一切真实的模式上报都被静默丢弃**，包括用户按厂商 Fn 热键切模式、以及 GCU 自己的回滚。界面会一直显示一个从未下发成功的模式。

修法要分清两种失败：**发布失败**要解除（命令根本没上路），**确认超时**不能解除（命令已上路，这时解除反而会让在途的旧包被当成新状态接受）。两条都有测试钉住。

**退出时可能反而打开了推流**

上一版加的 `System_OFF`（退出前让 GCU 停止采集）有一个竞态：`StopTelemetryBeforeExit` 看到 `IsConnected == false` 就直接返回。如果退出瞬间正好在断线重连中，`ConnectAsync` 已经越过了开头那次 `_disposed` 检查，会继续跑完订阅并发出 `System_ON`——我们进程都没了，GCU 反而刚被要求打开推流，一直白耗电到下次有人发 `System_OFF`。

两侧收口：`Dispose` 先拿 `_connectLock` 把 `ConnectAsync` 请下来再动 client；`ConnectAsync` 在发初始状态前后各复查一次 `_disposed`。

同一条命令还有个更直接的问题：它用的是默认的 QoS2。QoS2 要走四步握手，而这里只等 700 毫秒，超时后 `hw.Dispose()` 断开会话，`CleanSession=true` 让未完成的 PUBREL 被永久丢弃——命令等于没发，正是这段代码要解决的问题。现在这一条走 QoS0：只要写进 socket 就算完成，「停推流」这种清理动作丢一次的代价远低于卡住退出。超时后那个孤儿 Task 也补了异常观察，否则它会在 `Logger.Close()` 之后才抛。

**开机限充任务与托盘进程互踢**

`client id` 是写死的常量，但开机限充是同一个 exe 的独立进程，会和常驻托盘重叠。按 MQTT 3.1.1，broker 收到同 id 的新连接必须踢掉已有会话：限充连上踢掉托盘，托盘重连又踢掉限充，两个进程互踢直到限充那 10 轮轮询跑完。限充大概率失败，而托盘每次重连都 `generation++` 触发一整轮状态重放（全量刷新 + 灯效恢复 + 模式重应用）。

现在辅助实例用独立的 client id。

**两份握手序列分叉，而且 `System_ON` 排在末尾**

首连的握手与 `RefreshAll` 是两份几乎相同但不完全一样的手写列表——后者多一条 `LCHWOC/Control GETSTATUS`。当前不出错只是因为首连之后一定会触发一次 `RefreshAll` 把它补上，但两份并存意味着将来加新主题只改一边就会留下「首连缺某个状态」的间歇性症状。现在合并成一份。

合并时顺手把 `System_ON` 从倒数第二条提到**第一条**：它才是让 GCU 打开周期性传感器推送的开关，排在末尾的话前面那批 GETSTATUS 是在采集模块可能还没启动的状态下发的，于是要靠启动流程里的补发逻辑再跑一轮（额外 800 + 1200 毫秒等待）。本轮真机日志里那条「尚无温度遥测，补发全量状态请求」不再出现。

**假连接窗口 15-25 秒**

`KeepAlive` 从来没显式设置过，走的是 MQTTnet 的默认值。而 `IsConnected` 只在收到 FIN/RST 或 keepalive 超时时才翻转——GCUService 被冻结或强杀但 socket 没干净关闭时，`IsConnected` 能保持真值十几到二十几秒。那个窗口里每条命令都要卡到 5 秒的发布超时才失败，而切显卡模式一次发十几条且全程持锁，最坏累积到分钟级卡死。

现在显式设成 3 秒。取这个值是为了让**心跳先于发布超时**暴露断线（MQTT 的判定是 keepalive × 1.5，约 4.5 秒 < 5 秒）；反过来的话用户看到的永远是「点了没反应，5 秒后失败」，而不是一次干净的重连。

**订阅结果码被丢弃**

13 次订阅的返回值全部没看。SUBACK 里的失败码不会让 `SubscribeAsync` 抛异常，所以 broker 对某个主题做 ACL 拒绝时，我们会认为订阅成功、继续发 GETSTATUS，然后永久静默丢失那一类状态。原来的注释说要防「连接成功但零订阅的死态」，其实只防住了「抛异常」那一半。现在检查每个 filter 的结果码，有拒绝就断开重连。顺带把 13 次串行往返合并成一次多 filter 调用。

**状态解析的三类一致性问题**

- **`*Seen` 无条件置位**：`Settings/DeviceSwitchItemStatus` 的五项（屏幕亮度、触摸板、WiFi、蓝牙、摄像头）过去是「字段存在就置位」。固件写了个没见过的记法时，值会静默变成 false 而 Seen 照样置位——界面上长出一个点了没反应的开关。游戏白名单同型，而且它在能力判定里没有任何别的门禁兜底。正确写法本来就在同一个文件里（自动刷新率那一项）。
- **0 值被 latch 成「已知值」**：PL4 及其上下限、显卡 TGP、Dynamic Boost。这几处用的是「没报就保持旧值」的解析，所以 GCU 在功能关闭时报的 0 一旦被记住就再也回不到「未知」，而下游的 `>= 0` 守卫会让它通过——界面上是一个「已知的 0 W」，上下限都是 0 时滑条量程会永久塌成 0..0。CPU 功耗墙那一族早就修过同一个形状，这次补齐其余的。
- **三处状态串判定漏了忽略大小写**：来电自启、深度睡眠、Copilot 键锁。同族其余十几项都有，只有这三个没有——固件把值写成小写就会被判成相反的状态。

**事件触发的位置与隔离**

`HandleMessage` 末尾那三个通知早就做了逐订阅者隔离，但 case **内部**的事件还是裸调用。最危险的是 `ModeChanged`：它在 `Fan/Status` 中段触发，订阅者抛异常会把后面的功耗墙、温度墙、TGP、超频回读连同版本号一起丢掉，而那些字段是本帧最主要的内容。`CloseTimerChanged` 在 `Setting/Status` 中段，同理。

现在 case 内部的五个事件全部收进隔离调用，并挪到各自 case 的末尾（版本号自增之后）。这样的顺序有个额外好处：订阅者读到的一定是完整的一帧。

顺带把 `Setting/Status` 里的注册表读取单独包了一层。那是在 MQTT 接收线程上做阻塞式注册表 I/O，位置又在中段——它一抛，整帧连显卡模式一起丢。

**版本号的 release 语义**

`Setting/Status` 应该只有一个 release 点。显卡切换结果的版本号过去在 case 中段自增，而 `GpuMode` 要到十几行之后才发布，构成「新结果版本 + 旧模式」。目前靠确认谓词里的短路兜住（先判模式再看结果版本），不至于误判成功，但已下移到统一的发布点。校色状态的版本号则改成「真解析出值才自增」，而不是「字段存在就自增」。

**进程与资源**

- `CheckAlreadyRunning` 按进程名枚举时没有 session 过滤，是全机范围的。多用户或快速用户切换时，A 用户启动会去杀 B 用户会话里的同名进程，没权限就弹框把自己关掉——症状是「另一个登录用户开着，我这边就起不来」。官方控制台隔离那边早就做了这层过滤，两处做法不一致。
- `Dispose` 不清空公开事件。UI 审计路径会反复创建销毁实例，残留订阅会让已释放的窗体继续被持有。
- `_disposed` 的读改写不是原子的，两个线程同时释放会双双进入。退出路径有别的标志兜住，UI 审计那条路径没有。

**审计里确认过、但不需要改的地方**

记下来免得以后重复审：

- **订阅与首次 GETSTATUS 的顺序是安全的**。`SubscribeAsync` 是请求-响应语义，Task 完成意味着已收到 SUBACK，之后的回包必然命中已生效的订阅。`await` 本身就是防护，不需要额外机制。但这个隐含依赖值得写进注释——谁为了加速把它改成 fire-and-forget，首帧就会静默丢失，而症状是间歇性的。
- **`BatteryProtection/Control` 发到一个没订阅的主题，不是遗漏**。回读走的是 `System/BatteryProtection`，被 `System/#` 覆盖。请求主题和回应主题不在同一族。
- **退出路径上的 `.Wait(700ms)` 不会死锁**，虽然形状像经典的 sync-over-async。因为发布路径全程 `ConfigureAwait(false)`，没有捕获界面线程的同步上下文。这一点也写进注释了：将来谁去掉某个 `ConfigureAwait(false)` 就会变成挂死。
- **模式切换窗口不可能永久卡住**：过期判定用的是绝对时间，即使没人清除，8 秒后自动失效。
- **确认成功后不清除窗口是有意的**，用于继续拒绝仍在途的旧包。副作用是「切换成功后 8 秒内按厂商热键切别的模式」那一包会被丢，几秒后由 GCU 的周期推送自愈。

**验证结果**

Release 构建 0 warning 0 error；测试 1081 → **1124 全过**（新增 43 个）。

真机验证 54 项：48 通过 / 1 失败 / 3 不支持 / 1 需重启 / 1 按设计跳过。唯一失败是单色键盘灯，命令与官方逐字一致、本机服务端不响应，已确认非代码缺陷。

日志前后对照除了铰链灯带那条，刷新率切到 300Hz 这次也从 `confirmed=False` 变成 `confirmed=True`。**这一条我没有确证因果**：300Hz 在更早的几轮里也 Pass 过，属于历史偶发失败项。本轮把 `System_ON` 提前、KeepAlive 缩短、版本号 release 点收敛，都可能让 `GPUDevice/Status` 的回读更及时，但我没有做隔离实验，所以只能说「这次通了」。

CPU 温度墙仍判为不支持：服务端上报的范围自相矛盾（当前值 15 大于上限 10），换算后区间塌缩成一个点。这一项没有定位清楚，判为不支持、不暴露入口是当前的安全选择。

**已知问题：UI 几何审计在本版本不可复现（未修）**

我在本轮过程记录里写过一次「704 张 / 0 问题，属假警报」。**那是错的**，我看错了报告。实际数据：

| 运行 | 被测二进制 | 截图 | 几何问题 |
| --- | --- | --- | --- |
| `ui-audit-asus-cleanup`（8/29 14:53） | beta12 源码 | 704 | 0 |
| `ui-audit-plumbing`（8/30 21:02） | beta13 | 706 | 670 |
| `ui-audit-plumbing-r2`（8/30 21:14） | beta13 | 704 | 813 |
| `ui-audit-plumbing-r3`（8/30 21:37） | beta13 | 756 | 185 |
| `ui-audit-beta9.5-replay`（8/30 22:0x） | 8/23 留下的 `0.285.5-beta9.5` 二进制 | 640 | 0 |

- 同一份 beta13 构建连跑三次得到 670 / 813 / 185 个问题，截图数 706 / 704 / 756。**审计本身已经不可复现**，任何一次的绝对数字都不能单独当结论用。
- 三次都是干净运行。日志时间线确认 21:02–21:23 与 21:37 期间没有常驻实例并发（活实例日志出现在 21:46 之后，是我做实时抓图实验时启动的）。
- 宿主环境无罪：五次 `HostDpi` 都是 168；把 8/23 的历史二进制拿到**今天、同一台机器**重跑，仍然是 0 问题。所以回退在 beta9.5→beta13 之间的代码里，不是 Windows 或显示设置漂移。
- 差异集中在 `SettingsForm`：beta12 与 beta13 共有的 704 张截图里 401 张不同，且**全部**是 `Settings-*`（外加 `Donate`）；`HingeLight`/`Lightbar`/`LogoLight`/`SyncLight`/`KeyboardRgb`/`FanCurve`/`ColorCalibration` 等逐字节相同。两次同源运行相比（r2 vs r3）433 张不同，仍以 `Settings-*` 为主。
- 症状是继承 `SettingsForm.Font` 的文字相对控件框偏大：`夜间` 只剩 29px 可用宽（需 44–46）被裁成「夜」，`屏幕校色` 塌成「屏」，`100%` 折成两行。显式设过字体的控件不受影响——页脚 `赞助/更新/退出` 与 `ApplyTitle` 的 10pt 标题在两版里像素一致。beta12 的标题/正文字高比约 19:17，正合代码里的 10pt:9pt；beta13 变成标题比正文还小，说明变的是继承来的那一份。
- 机制指向 `SettingsForm` 的布局反馈环：窗体是 `AutoSize=GrowAndShrink`（`Settings.Designer.cs:1351`）、页容器 `AutoSize=true` + `Dock=Top`，而 `ArrangeDashboard` 又用 `ClientSize.Width` 反推列数（`Settings.cs:2753`）。宽度→列数→高度→自动尺寸→宽度构成回路，`Application.DoEvents()` 之后跑了几趟布局就决定它停在哪个状态。这也解释了截图数为什么会变——`CapturePages` 按窗体高度决定是否切成 top/middle/bottom 三张。
- **没有定位到本轮 5 个改动文件里的哪一个触发了它**，而且反证很强：`--ui-audit` 在任何 MQTT/hw 初始化之前就 `return`；`Settings` 工厂的 `Program.hw` 为 null，本轮改的能力解析根本进不去这条路径；`RaiseIsolated` 是纯同步的，且审计里 `HandleMessage` 在窗体创建**之前**调用，此时没有订阅者，等于空转。两个待查方向：（1）`config.json` 在两次审计之间被真机验证改写过（`frequency` / `max_frequency` / `screen_max` 决定刷新率面板的按钮个数，直接喂给上面那个宽度→列数回路）；（2）beta12 那次 0 只是恰好落在稳定点上。
- 顺带两个坑：`--ui-audit` 不经过 `ProcessHelper.CheckAlreadyRunning`，审计进程能和常驻实例共存；而 `test-ui.ps1` 传了 `GITHUB_ACTIONS=true`，csproj 里的 `StopRunningLMechrevo` 目标会被条件跳过，不会替你关掉在跑的实例——跑审计前得自己确认。
- 判定阈值只留 2px 余量（`UiAuditRunner.cs:406`、`:413`）。**不要**为了让报告变绿去放宽它，那是改测量工具而不是被测对象。

**测试套件的一次间歇性挂起（未复现，未定位）**

22:00 前后连续 5 次 `dotnet test` 卡在 `A total of 1 test files matched` 之后，一个结果都不产出。单独跑任何一个测试类都正常，8 个可疑类成组跑也正常。此后连跑 5 轮完整套件全部 **1124/1124 通过**（每轮 79 秒），再没复现，也没抓到托管栈。那段时间的共同前提是我刚用 `Stop-Process -Force` 强杀过活实例，怀疑强杀留下的 broker 会话或内核模块句柄卡住了集成测试，但**没有证据**。记在这里以防再遇到。

## beta12（2026-08-28）

这一版只做一件事：**把继承自 g-helper 的华硕专有代码清出去**，同时保证机械革命侧的功能一行都不少。

净删除约 2100 行。判断标准不是"名字里有 Asus"，而是"在机械革命机器上是否真的可达"——按名字批量删会误伤四处活代码，下面单列。

**为什么必须清，而不是"留着也没事"**

清理前这些代码的状态是：**每次启动都在调用，但全部落到空实现**。可达性靠层层恒 false 的门禁维持，而不是显式声明。有两条实际危害：

第一条是误导。`Stubs.cs` 里那批空存根（方法体全是 `{ }` 或 `=> false`）签名对齐 g-helper 原文件，看起来像一套可用的 API。谁照着它接线，就会得到一个点了没反应的开关——这一版之前刚花了一整轮审计在修这类问题。

第二条是正确性风险，有具体实例。`AppConfig.IsChargeLimit6080()` 的机型候选清单里有 `"H760"`，命中时它会在机械革命的电池三档对齐**之前**先按华硕那套改一遍充电上限：华硕是 `>85→100 / >=80→80 / <60→60`，我们是 `>=95→100 / >=80→80 / else→60`。机型串一旦意外命中，用户设的百分比就会落到与预期不同的档位。这条分支从来没被验证过，因为在开发机上永远不执行。

**删掉的五族**

- **ROG Ally 掌机**：`AllyControl` / `ControllerMode` / `Handheld` 存根，三个按钮处理器，四个可视化方法（其中 `VisualiseAlly` 的签名就是 `bool visible = false` 加首行 `if (!visible) return;`——连自己都默认不做事），以及设计器里的 `panelAlly` 子树。
- **机盖点阵屏与 Slash 灯条**（Anime Matrix）：`AniMatrixControl` / `Matrix` / `SlashDevice` / `SlashMode` 存根，七个界面处理器，`panelMatrix` 子树。这一族其实早就跑不起来：`InitMatrix` 首句 `if (!matrixControl.IsValid)` 就返回（存根恒 false），所以那几个 `CheckedChanged` 从来没被挂上；唯一能创建窗体的入口挂在一个 `Visible = false` 的按钮上。
- **XG Mobile 外置显卡坞**：`XGM` 存根、`GPUModeControl.ToggleXGM`、`VisualizeXGM` 两个重载、`buttonXGM`、启动路径上的三处空调用。`ToggleXGM` 留着的风险不只是没用：它会弹一个英文对话框、再 `Task.Delay` 15 秒锁住 GPU 面板。
- **ASUS Splendid（GameVisual）色域引擎**：整个 `Display/VisualControl.cs`（530 行）与 `Display/ColorProfileHelper.cs`（707 行，一张华硕机型到 ICC 下载地址的映射表，零外部调用方）。Splendid 的唯一执行入口是 `AsusSplendid.exe`，只能经华硕的 ATKWMIACPIIO 驱动定位，色域 ICC 也只存在于华硕的 GameVisual 目录——机械革命上这些前提全部不成立。界面侧的 `panelGamma` 子树一并删除。
- **按机型串匹配的谓词族**：`IsAlly` / `IsTUF` / `IsVivoZenPro` / `IsDUO` / `IsSlash` / `IsSlashLong` / `IsZ13` / `HasRearLight` / `IsChargeLimit6080` / `IsAMDiGPU`，以及 `Modes.InitFullSpeed`（给 Vivobook 自动建 "Full Speed" 档，首句 `if (vivoMode < 0) return;` 永久生效）、机背灯整族（Z13 专属）、六个华硕专有显卡设备码、`AsusFan.XGM` 枚举成员。

**一个顺带发现：那批面板根本不在控件树里**

清理时才注意到 `SettingsForm` 只把 6 个面板 Add 到窗体上（性能 / GPU / 电池 / 版本 / 页脚 / 启动）。`panelMatrix`、`panelAlly`、`panelScreen`、`panelKeyboard`、`panelGamma`、`panelRearLight`、`panelPeripherals` **全都没有被 Add**。

也就是说那个 `foreach (Control legacy in ...) legacy.Visible = false;` 的隐藏循环完全是多余的——它在给不在窗体上的控件设可见性。这也解释了为什么删掉两棵面板树之后 UI 审计的截图基线一张不差。

**两个判据从"机型名"换成"配置开关"而不是删掉**

有两处的**功能语义**对机械革命有意义，只是判据是华硕专有的，所以保留入口、换掉判据：

- `IsOLED()`：原来是"机型串命中一张 30 多项的清单"或"华硕 OLEDCare 注册表里 `EnablePixelRefresh` 非零"。机械革命确实有 OLED 屏机型，但目前**没有可靠的判据来源**——GCU 状态帧里没有面板类型字段，注册表里也没有对应能力位。现在只保留手动开关，等拿到真实判据再接。唯一消费者是 AMD 驱动侧的 OLED 省电优化。
- `IsDynamicLighting()`：原判据是"2024 款华硕机型"。动态照明本身是 Windows 11 的通用特性，不是华硕专有，同样换成配置开关。

**顺手修掉一个一直没生效的对齐**

电池滑条的可选值原来由 `IsChargeLimit6080()` 决定，那个谓词恒假，所以滑条一直是连续的 40..100——用户能选到一个硬件到不了的百分比。现在直接对齐到三档的实际生效值（60 / 80 / 100）。

**按名字批量删会误伤的四处（已用测试锁住）**

这是这一版最需要记下来的部分。以下四处**名字看起来像华硕、实际是活代码**：

| 看起来像 | 实际是 | 误删后果 |
| --- | --- | --- |
| `RyzenSmu` 里的 `StrixPoint` / `StrixHalo` | **AMD Zen5 的代号**（Ryzen AI 9 / Ryzen AI MAX），活的 SMU 信箱路由 | 直接打断 AMD 机型的 TDP 与曲线调压 |
| `KeyboardRgb.ModeMatrix` / `MatrixSpeed` | 机械革命键盘 RGB 的固件灯效档位 | 键盘灯效少一档，且有测试覆盖会红 |
| `AsusACPI` 类本身 | 活的适配层：把继承来的界面调用转成 `MechrevoHw` 操作，风扇曲线读写就走这里 | 整个风扇曲线功能失效 |
| `AsusFan` 枚举 | `CPU` / `GPU` 是风扇曲线映射的入参 | 同上 |

另外 `IsXGConnected()` **保留并恒 false**，那是有意的显式否认而不是漏改：机械革命全系没有 XG Mobile 接口，恒 false 就是正确答案，删掉会让调用方失去这个语义锚点。

`Overlay`（硬件状态悬浮窗）也差点被误删——它的按钮和 ROG Ally 的两个按钮同处一个不可见的表格里，但功能本身是活的，入口在动作面板的"悬浮监控"。

还有一处概念混淆要注意：被删掉的 `sliderGamma` 调的是 Splendid 的 gamma **假调光**，不是屏幕亮度。真实亮度走 `Settings/DeviceSwitchItemStatus` 的 `ScreenBrightness` + `BrightnessCommitQueue`，两者无关。

**测试**

新增 59 个（1022 → 1081）。这一组的写法有点特别：**用反射断言符号不存在**，而不是编译期引用。原因很直接——编译期引用一个已删除的类型会让测试项目编译失败，测试根本没机会跑。

除了"这些必须没了"，同样重要的是反向断言"这些必须还在"，每条都写明为什么。上面那张误伤表里的四项各有一条守卫。

**验证结果**

Release 构建 0 warning 0 error；1081 个测试全部通过、0 跳过。

UI 审计 16 个视口 704 张截图 0 问题。截图数比上一版的 702 多两张，是删掉不可见面板后布局高度变化带来的滚动分屏差异，不是新增界面。

真机验证 54 项：47-48 通过 / 1-2 失败，与上一版持平。跑了两轮，两轮之间的差异全部落在两个时序敏感项上，稳定失败的只有单色键盘灯那一项（命令与官方逐字一致，本机服务端不响应，已确认非代码缺陷）。

删除量：`Settings.Designer.cs` 2235 → 1486 行，`Settings.cs` 4934 → 4767 行，`Stubs.cs` 213 → 153 行，`GPUModeControl.cs` 544 → 499 行，另有两个文件整体删除（1237 行）。

**两个时序敏感项，顺手改了验证器**

- **恢复原模式**：连续切了三个模式之后，第四次（切回自定义）的回读没能在 `SwitchMode` 的确认窗口（180+700+1000 毫秒）内变过来。而紧接着的自定义档切换是成功的——说明服务端没问题，是这一轮的等待不够。验证器的恢复步骤原来只判一次，现在允许重试一次，与快捷开关那边的恢复逻辑对齐。单次判定会把时序抖动报成功能缺陷。
- **刷新率切到 300Hz**：`currentHZList` 报了 `[60, 240, 300]`，但切 300 有时不生效、回读仍是 240。这一项**还没定位**，两轮里只有一轮复现。

## beta11（2026-08-28）

这一版没有新功能规划，全部来自两件事：**用户报的两个现象**（性能/显卡模式按钮闪烁、很多开关点了没反应），以及为了查清第二个现象而做的一次全工程审计。

结果是修掉了 9 个「点了没反应」的空头开关、接上了 5 个「协议层早就写好但界面从来没接」的功能、删掉了 7 个会误导后来人的死方法。真机验证从 **38 通过 / 8 失败** 变成 **48 通过 / 1 失败**，剩下那 1 个已确认不是我们的代码问题。

**按钮闪烁：重排时无条件重建控件树**

性能模式与显卡模式的按钮放在 `TableLayoutPanel` 里按可见数量重排。原来的实现每次刷新都把控件全部移除再加回去——哪怕这次的布局和上次一模一样。`Controls.Clear()` + `Add()` 会触发一轮完整的重绘，定时器每秒刷一次，看起来就是持续闪。

修法是给重排加布局签名（控件数量 + 控件名有序拼接），签名没变就整个跳过，不动控件树。三处调用（性能按钮、显卡按钮、灯效按钮）各持一份自己的上次签名。

签名里的数量前缀不是冗余：没有它的话「这次是空行」的签名会与「从未布局过」的初值撞上，第一次真实布局会被跳过。这条是测试抓出来的。

**为什么要写一个真机验证器**

「有很多功能开关之后没任何反应」这句话没法靠读代码回答。命令发出去了、方法返回了 `true`、测试也绿——但硬件那边到底有没有变，只有在真机上把每一项开关都点一遍、再读回来才知道。

所以做了 `FunctionVerifier`（受 `HARDWARE_DIAGNOSTICS` 保护，入口 `--verify-functions <报告路径>`）：逐项读基线、往相反方向下发、回读、恢复原状，输出 JSON 报告。几条纪律：

- 每一项必须恢复原状，不能给用户留下改动。
- 结论分五档：通过 / 失败 / 不支持 / 按设计跳过 / 需重启生效。把「这台机器没有这项能力」和「有能力但点了没反应」混在一起，报告就没有价值了。
- 有副作用不可接受的项目明确标注为「按设计跳过」，不偷偷略过——显卡模式切换需要重启，而且失败可能导致没有显示输出，不适合自动化。
- 报告里附带原始报文转储。这一条后来成了整轮审计的关键：**代码里的字段名与服务端实际发出来的名字之间的偏差，只有把两边摆在一起才看得见。**

**九个空头开关，八个是名字对不上**

有了原始载荷转储，就可以做一件之前做不到的事：把代码里引用的每一个字段名，与实测载荷里的 93 个（`Fan/Status`）和 53 个（`Setting/Status`）字段名做交叉比对。一次抓出了全部偏差。

- **CPU 高级性能：三处全错。** 状态字段是 `CPU_PerformanceAndOverClockMenuSwitch`——**带下划线**。我们读的是没下划线的那个（那是服务端内部结构体的成员名，不是发出来的 JSON 键）。于是这一项的能力判定恒假，**界面上从来没出现过**，也就没人报障。下发同样错：官方走 `Fan/Control` + `Action=SET_OPERATING_MODE_DETAIL`，开与关的区别在**载荷字段名**上（`..._ON="1"` / `..._OFF="0"`），我们把那两个字段名当成 `Action` 发到了 `Setting/Control`。确认路径也错：状态在 `Fan/Status` 里，而我们用轮询 `Setting` 通道的确认器去等，永远等不到。
- **能力门禁的依据找错了地方。** 原来只认构造时从注册表读的一个键，而这台机器的注册表里根本没有那个键。官方的判据是「注册表 `OcSettingsSupport`」或「`LCHWOC` 报的 `Support`（默认真）」任一成立。现在改成三份证据取或，其中一份是状态帧里随帧刷新的 `OcSupport`——那是唯一会跟着硬件变的那份。
- **电源灯开关恒显示为关。** 值是状态字符串 `PowerLight_ON`，而我们用布尔解析器读它。那个解析器归一化后做精确匹配，`POWERLIGHTON` 匹配不上 `ON`，于是永远返回「无法识别」。
- **游戏白名单的主题、动作、值三处都不一样。** 它走 `Fan/Control`（不是 `Setting/Control`），动作名 `OPERATING_GAME_WHITE_LIST` **没有** `_ON`/`_OFF` 后缀，开关值在载荷字段 `GameWhitelistSwitch` 里（数字 1/0）。
- **一组状态串判定与官方不一致。** 官方读的是「串里有没有那个否定词」（`!Contains("OFF")` / `!Contains("UNLOCK")`），我们是拿整串比常量。在这台机器上两种写法结果相同，所以之前没暴露——但固件一旦把 `USB_CHARGER_STATUS_ON` 写成 `USB_CHARGER_ON`，精确匹配就永久判成关闭。已改为逐字对齐官方，测试特意用**变体名**断言（用实测的那个名字反而测不出退化）。
- **屏幕响应加速暴露了一个本机不具备的入口。** 服务端对不支持的机型照样发状态字段：实测 `LCDOverdriveSwitch=LCDOverdrive_OFF` 与 `LCDOverdriveSupport=NotSupport` 同时出现。原来无条件认为「报了字段就是有这项能力」，于是长出一个点了永远不生效的开关。现在支持位显式为假时一票否决，连注册表里的静态画像也否决掉——注册表是机型出厂画像，可能与实际装的屏不一致。

顺带纠正三处：`LocalDimming` / `LcdOverdrive` 的值判定官方**本来就是**精确匹配（那两个动作名里没有否定词，改成子串匹配反而没依据），保留；灯带电源回读官方也是精确比 `"On"`，只放宽到忽略大小写（值域只有 On/Off，不会误判，但能兜住固件写成 `"ON"`）；刷新率的 `Hz` 官方发的是字符串，我们的写法本来就对。

**GPU 静音模式：撤掉入口**

服务端把这一族完整报上来（支持位、开关、档位、三档最低帧率都有），看着像个能做的功能。但官方 5.56 界面里对应的两个属性**只有声明、零下发点**——拿不到命令的真实形状。

之前按枚举名猜了一个形状实现出来，真机验证的结论是下发后回读毫无变化。而且当时把 `Switch`（开关）和 `Setting`（静音档位）搞混了。现在入口撤掉、状态解析保留供诊断，并写明「除非拿到真实的下发点，这一族必须保持只读」。测试也从「断言它可用」改成「断言它永远不出现在可写开关字典里」。

这是这一版第二次用同样的方式踩坑（第一次是 beta10 的第三颗风扇）。纪律再确认一遍：**按枚举名猜命令形状，两次都被真机证伪。**

**五个「协议层写好了但界面没接」的功能**

这是空头接口的另一种形态：不是点了没反应，而是根本没有可点的地方。

- **小键盘锁**：解析、能力判定、下发动作三段一直都齐，唯独漏了界面入口——于是那三段全成了死代码。
- **电池 Logo 灯**：字段与命令完全缺失，有这条灯的机型在我们这边看不到入口。
- **Logo 灯带开关**：四条灯带里唯一没有快捷开关的，之前只能进灯效面板才能关它。
- **外接屏断独显 / 电池切独显**：命令与状态解析早就有，从来没接到界面上。而且这两个方法原来是「Publish 完就 `return true`」——把「已发送」当成「已生效」，现在补上了回读确认。

`GPU_POWERSAVEINGMODE`（官方 GPU 设置页的「应用」按钮）**故意没接**，理由写在代码里：它是一次性动作、没有状态可回读，效果是触发一次独显工作模式切换，与我们已有的显卡模式面板语义重叠。多一个按钮只会让用户在两处切显卡，而显卡切换失败可能连显示输出都没有。

**回滚与抑制：三处会让勾选框自己跳动的地方**

- **命令失败后的回滚是取反而不是重读硬件。** 取反的隐含假设是「失败等于什么都没变」，这不成立：互斥开关失败时伙伴可能已经被改掉了，而界面与硬件本来就可能不同步（回显有 12 秒抑制窗）。现在按硬件此刻的真实状态回滚。
- **Uni / Omni 半成功时不还原伙伴。** 这一组是互斥单选。先关伙伴成功、再开自己失败时，用户点了一下 Omni，结果 Omni 没开、Uni 反而被关掉了，落到「两个都关」——一个本来不该出现的组合。
- **深度睡眠时长下拉是全文唯一不检查回显抑制标志的写命令处理器。** 回显把下拉设成硬件当前值时会再触发一次下发。之所以一直没出事，只是因为「新值等于硬件值就不发」恰好把它挡住了；一旦回显和硬件差一格就会自己发命令。同时它的返回值被直接丢弃：命令没生效时下拉还停在用户选的那一档，界面上看起来改成功了。

**切模式时超频通道不复位**

官方每次切模式都发两条：`Fan/Control` 的模式动作，紧跟 `LCHWOC/Control` 的运行标记（游戏=1 / 增强=2 / 办公=0，自定义档用另一个字段）。我们只在切自定义档时发过那条标记、从不发普通模式的，于是从自定义模式切回办公模式后，硬件那边的超频通道还留在「自定义运行」状态、参数不复位。

顺带把 `ProfileIndex` 从字符串改成 JSON 数字（官方发的是整数），并让两条切模式路径共用同一份载荷构造——之前是两份手写载荷，其中一份漏了 `LCHWOC` 那条。

**退出时不停止遥测推流**

官方在窗口关闭、失活、切页时都会发 `System/Control {Action=System_OFF}`（共 7 处），让服务端停止采集。我们只发过 `System_ON`，从不发 `OFF`：进程都结束了，服务端还在按 5 秒周期采集并推送传感器数据。现在退出前尽力发一次，700 毫秒超时就放弃——为一个清理动作拖慢退出不值得。

**删掉第二张动作表**

`MechrevoHw` 上有 7 个与服务层平行的下发方法（快捷开关、USB 充电、风扇增强、刷新率、三个 GPU 命令），只发不确认，**零调用方**。

留着的害处不是占空间，是它会静默腐化：那张快捷开关表停在 11 个开关，后来补的 8 个官方开关一个都没有，而且其中两个根本不该走 `Setting/Control`。谁要是照它接线，就会得到一个点了没反应的开关。全部删除，并留注释说明需要下发就走服务层——那边每条命令都带回读确认。

**测试**

新增 53 个（969 → 1022），全部通过。这一批测试有个共同的设计约束值得记下来：**载荷片段全部取自真机原始报文转储，不是照代码反推的。** 上面那八个「名字对不上」的缺陷，只要测试跟着代码里错的那份名字写，就会跟着一起错、照样绿。

同一个坑在写测试时又踩了一次：`Settings/DeviceSwitchItemStatus` 的字段名实际是 `TochpadEnable`（少一个 u）、`WIFIEnable`、`BTEnable`。第一版测试写成了「看起来对」的 `TouchPadEnable` / `WifiEnable`，结果那四项被静默跳过——测试照样绿，但什么都没验证。

另外加了一条反向断言：**只要能力判定认某个开关，就必须有能把它发出去的路径。** 小键盘锁那种「三段齐全独缺入口」的情况，从这个方向能抓住。

**验证结果**

Release 构建 0 warning 0 error；1022 个测试全部通过、0 跳过。

真机验证 54 项：48 通过 / 1 失败 / 3 不支持 / 1 需重启 / 1 按设计跳过。上一轮的 8 个失败项里，电源灯、游戏白名单、CPU 高级性能全部转为通过，GPU 静音已撤入口，深度睡眠归类为需重启生效，功耗墙与显卡 TGP 的数值写入在补了「先切自定义模式」之后不再失败。本轮新接的 5 项全部通过。

唯一残留的失败是单色键盘灯：命令与官方逐字一致，本机服务端不响应。这一项**保留入口**——撤掉会误删一个正确的实现，有这套硬件的机型上它是能用的。

CPU 温度墙判为「不支持」，原因是服务端上报的范围自相矛盾（当前值 15 大于上限 10），换算后区间塌缩成一个点。这一项没有定位清楚，判为不支持、不暴露入口是当前的安全选择。

## beta10（2026-08-28）

这一版的主题是**从「在这台笔记本上能跑」改成「按适配全系机型来写」**。做法分三条：把所有「本机实测为……」的硬编码假设换成按机型画像回答；把官方控制台有、我们没有的功能补齐；从三个官方控制台里把全机型的 EC 与风扇表对照数据提取出来，为将来脱离厂商服务、直连硬件铺路。

同时明确了一条纪律：**协议取不到证据的功能不实现**。下面有四项功能是查证之后决定不做的，理由逐条写在 `docs/hardware/README.md` 里——记录「为什么不做」和记录「做了什么」一样重要，否则以后还得再查一遍。

**全机型正确性：消除单机硬编码**

- `AsusACPI` 里六个「本机实测为……」的硬编码应答改为按机型画像回答：`IsNVidiaGPU`、`IsAllAmdPPT`、`IsOverdriveSupported`、`IsSupported(设备码)` 走能力位。`IsXGConnected`（华硕外置显卡坞）与 `IsMidFanSupported`（中置风扇）保持恒 `false` 并注明理由：这两样都是 g-helper 的华硕硬件概念，机械革命侧没有对应物，恒 false 是正确答案而不是未适配。
- `MechrevoDeviceCapabilities` 新增进程级共享快照与 `Invalidate()` / `OverrideCurrent()`，让能力画像可以被测试注入不同机型。
- `AsusACPI` 的功耗常量补了注释说明它们是**外层安全边界**、不是机型限值：面向用户的范围必须用 `MechrevoHw` 的运行时 `Pl1Minimum/Maximum` 等。差距有多大有实据——开发机上限 210 W，而出厂表里 PH4TRX1 是 PL1=35 / PL2=60。
- 新增多机型能力矩阵测试：构造与开发机不同的画像，任何一处退回硬编码就会失败。

**查清「中置风扇」这件事：这套协议里没有第三颗风扇的读数**

`HardwareControl.midFan` 有字段声明、界面也有读取，但从来没有赋值点。这一版一开始按「补上缺失的那一环」处理，在 `System/FanInfo` 里加了第三颗风扇的解析——**这是错的，已经撤掉**。

错在字段名是自己编的（`MidFanDuty` / `RamFanDuty` / `ThirdFanDuty` / `Fan3Duty` / `MiddleFanDuty`），一个都不存在于协议里。于是那条路径依然是死的，只是从「没有赋值点」变成了「赋值永不命中」，而代码看起来像接上了。

查证结论：

- 官方 `System/FanInfo` 的处理只读四个字段：`CpuFanDuty`、`GpuFanDuty`、`CpuFanRpm`、`GpuFanRpm`。
- 官方 58 个 MQTT 主题里与风扇有关的只有 `System/FanInfo` 与 `System/FanErrorInfo`。
- 官方界面是全机型共用的，只显示两颗风扇——所以没有哪个机型会报第三颗。
- 内存风扇（`RamFan1p5Support`）确实存在，但 EC 侧只有三个**风扇表**寄存器
  （`ADDR_RAMFAN1P5_TABLE_STATUS1/2`、`TABLE_CTRL`），**没有转速与占空比寄存器**；
  它由 GCUService 内部随风扇表自动管理（`MyFanTableCtrl.SetFanControlByRamFan1p5`），
  官方控制台自己也没有读数和控制入口。

顺带纠正两个概念混淆：`RamFan2_ECSpec` 的 `TABLE_F1/F2/F3` 是**三套风扇表**（每套都含 CPU 段和 GPU 段），不是三颗风扇；`ADDR_MYFAN3_*` 里的 "MyFan3" 是风扇管理**代次**（MyFan1/2/3），也不是第三颗风扇。

所以「中置风扇」是从 g-helper 继承的华硕概念，在机械革命这套协议上没有对应物。现在 `midFan` 恒为 `null`、`MidFanSeen` 恒为 `false`，并在三处（`MechrevoHw`、`HardwareControl`、`AsusACPI.IsMidFanSupported`）写明理由，让「不存在」显式可见。要恢复这块显示，前提是先拿到真实存在的字段名或 EC 寄存器，而不是再编一批名字。

**补齐官方有、我们没有的开关（8 项）**

触摸板切换键、单色键盘灯、Uni / Omni、电源灯、游戏白名单、CPU 高级性能、GPU 静音。判据一律是「服务端报过对应字段」，机型没有就自动隐藏，不靠机型名单。其中两项有额外约束：

- Uni / Omni 是互斥单选，开一个必须先关另一个，否则服务端会留下两个都开的状态、界面与硬件对不上。
- GPU 静音要求显式的 `GPU_WhisperModeSupport == true`：官方对不支持的机型也会带上设定字段，只看「字段存在」会造出假开关。

**补齐从未订阅的遥测**

第三颗风扇转速与占空比、电池循环次数与设计容量、网络吞吐、EC 固件版本、风扇异常标志。这些官方推流里一直带着，官方界面反而不展示——电池循环次数比 `powercfg /batteryreport` 实时得多。

**PL4 与风扇转换灵敏度**

- PL4（瞬时功耗墙）此前只解析了 `CPU_PL4_Double_Flag` 这一个标志位，值和范围都没读、也不能写。补齐时最容易错的是**半瓦换算**：官方对带这个标志位的机型，回读时把值与上下限一律乘 2、下发时折半。漏掉任何一侧，PL4 的显示或写入就差一倍。线上原始值单独留存，否则把公开属性当下一帧的回退值会逐帧复乘。
- 风扇转换灵敏度（换挡延迟毫秒数）两个字段全库都没解析过。GCU 不上报范围，所以范围按 EC 字段宽度推导——那是个单字节、单位 100 ms 的计数器，于是有效值是 100..25500 的百毫秒整数倍；将来固件真的上报了范围就以上报值为准。写入前先对齐到整档，否则会拿一个固件永远回读不到的值去比对，把成功的写入判成失败。
- 这一项归到自定义模式的风扇调参、走既有的批量提交路径，没有混进系统快捷开关——混进去会在快捷开关面板留下一个没有动作映射的幽灵项。顺带把批量提交的排序谓词从「`Switch` 后缀」放宽到「`Switch` 或 `Enabled` 后缀」，保证同一批里总闸先于数值下发。

**铰链灯带与同步灯带**

官方决定灯效页显示哪几条子灯带，用的是 `HidLightbar/Status` **载荷自带的**能力位（`LogoSupport` / `HingeSupport` / `BaseSupport` / `NewlogoSupport`），再按 `BIOS_PROJECT_ID` 过滤一层机型表。我们不复刻那张机型表——表上没有的新机器会被漏掉。判据改成设备自己报的能力位，加上「这条子灯带的状态主题有没有到过」。同步灯带在官方状态里没有对应的能力位，所以只能靠后者。

灯效入口从固定三个变成最多五个（键盘 / 灯条 / Logo / 铰链 / 同步），按当前可见数量重算列数，只支持三种的机器不会留下空格子。临时熄灯前的电源态快照也从两个独立字段改成按主题的字典，四条通道走同一段代码。

**电源灯亮度**

服务层方法早就存在但**没有任何调用方**。这次补了界面，并修掉两处协议错误：

- 调亮度原本借用开关动作（`PowerLight_ON` / `_OFF`），会连带重写开关态；官方另有专用的 `PowerLight_Brightness`，现在用它。
- 开关命令原本只发动作、不带亮度，而官方的开关载荷是**同时带亮度**的——不带的话服务端按 0 处理，等于开灯的同时把它调暗到看不见。

界面用离散档而不是连续滑块：每条命令都要等服务端回读确认，拖动连发会互相排队超时。

**任务栏自动隐藏 / 透明效果 / 深色主题**

这三项官方也有，且完全不走 MQTT：自动隐藏是 Shell 的 AppBar 状态，另两项是个性化注册表值，改完要广播 `WM_SETTINGCHANGE` 否则已运行的程序不重绘。深色主题必须同时写应用主题与系统主题两个值，只写一个会出现应用变深色而任务栏还是浅色的割裂。

有一处我们比官方更正确：`ABM_SETSTATE` 的参数是**整个状态字**，官方直接写 1 或 0，副作用是顺手把「任务栏置顶」也清掉了。我们先读现状、只改自动隐藏那一位再写回。

因为是 Windows 的设置而不是硬件能力，这三项不受 GCU 连接状态影响，未连服务时也会跟随外部改动刷新。

**功耗墙生效校验**

GCU 回读一个 PL1 只证明「服务端记下了」，不证明「CPU 照着做了」。新增的校验拿实测封装功耗去对，是这条链路唯一的端到端证据。

关键是**不能做成简单比大小**：PL1 是持续限值、带一个时间常数（Intel 默认约 28 秒），短时冲到 PL2 是设计行为不是故障；而轻载时功耗远低于上限，这种情况什么都证明不了。所以判定只看 60 秒窗口上的平均值，且窗口必须真的铺满；负载没把墙顶起来时老实回答「判不出来」，而不是报「正常」。只有明确判出没生效才会提示——「判不出来」是常态，不该拿它去吓人。

**电池健康信息**

循环次数、设计容量、电池异常标志接进电池面板标题。全部未知时不显示——占位值比不显示更糟。

**全机型 EC 与风扇表对照数据（为直连铺路）**

`docs/hardware/` 下五个机器可读的数据文件 + 可复现的提取脚本 + 一份说明。核心数据：258 个 EC 寄存器槽、33 个 IOCTL、387 个结构字段、45 项 `ProjectID`、37 项 `BIOS_PROJECT_ID`、22 种键盘分区、24 机型 × 6 张出厂风扇表 × 3 个版本。

这批数据能提出来是因为官方服务虽然被 ConfuserEx 处理过（4,574 处方法体被替换成抛异常），但**常量、枚举、结构声明全部保留**。所以这不是逆向未知协议，而是给已知的表消歧。

消歧靠的是「所属 spec 类 + 能力位」而不是猜：40 组跨类地址复用被类型结构消掉，真正有歧义的只剩 15 组，且每一条的消歧依据都能从注册表能力位直接读到。

**数据可信度说明**：目前是「声明级可信、抽样级验证」。已做的验证是只读 EC 采样加与服务上报值交叉比对：电池百分比、PL1/PL2/PL4、风扇转速都对上了，但 `0x744` 读到 70 而实际 TGP 是 150 —— 这个地址同时被声明为两个含义，现场证明了**照着常量名直接写会写错寄存器**。所以文档里明确写了：动手写入之前必须先完成全量只读快照与双写对照两个阶段。

**查证后决定不实现的四项**（理由详见 `docs/hardware/README.md`）

- **内存风扇 1.5 调速**：`RamFan1p5Support` 不是用户可调项，而是官方服务内部选择 EC 寄存器布局变体的开关。三条证据：该 spec 类只比基础版多三个寄存器、六张风扇表基址完全相同；官方界面里那个看起来像内存风扇的类名其实是混淆器改名后的状态载荷类；遍历官方全部自定义参数载荷，没有任何内存风扇字段。调速只能走 EC 直连。用户能看见的部分——这颗风扇的转速与占空比——已经接上了。
- **连续充电阈值**：命令和 EC 地址都确认了（上限 `0x7B9` / 下限 `0x7D0`），但官方 5.56 界面从不发它、服务里的处理函数被桩化取不到字段名、服务内部还存在两套互不兼容的模型（上下阈值对 vs 枚举可选值列表），而且没有可用于确认的回读字段。盲写等于对充电控制器下一条语义未知的命令。充电上限仍走有回读确认的电池保护三档。
- **显示色彩模式与 NVIDIA 全局首选显卡**：整套命令在官方 5.56 里已弃用——动作名只出现在枚举声明里、零发送点，那批颜色字段也零读取点。加上两个命名陷阱（状态字段的拼写与命令名不一致；两个官方二进制里的动作名互不相同），无法判断服务端到底接受什么。这一族改为只解析、不下发。
- **风扇异常保护旁路**：官方界面没有任何入口去写它。

**顺带修掉的缺陷**

充电上限此前把用户请求的百分比**原样**存进配置并显示，而硬件只有三档，于是设 70% 界面显示「70%」、硬件其实停在 60%——界面报了一个这台机器到不了的值。现在统一对齐到档位的实际生效值。两个边界带写进了测试：低于 60% 是硬件下限（请求 40% 只能给 60%），95% 及以上按就近落到满充档。

**测试**

- 新增 184 个测试（762 → 946），全部通过、0 跳过。
- 新增的每一条都写明了它在防什么退化，而不只是断言当前行为。举几个例子：PL4 的半瓦换算不能被自己的输出污染（否则第二帧就变 4 倍）；风扇灵敏度必须对齐到 EC 整档（否则成功的写入会被判成失败）；同步灯带的支持不能从铰链的能力位推断（否则只有铰链的机型会多出一个假入口）；官方状态字段的拼写错误必须原样保留（照命令名去读会一个都读不到）；功耗墙在轻载时必须回答「判不出来」。
- 两条纪律值得记下来：写测试时先写了一个「充电上限对齐后永不超过用户请求值」的不变量，结果被两个真实边界抓出来（95%→100% 是就近取整、40%→60% 是硬件下限），说明当时的断言比现实更强，已改成真实成立的形式；任务栏与主题那三项的写入路径用「把当前值再写一遍」验证，完整走一遍写入加广播加回读，但不会改变用户的系统状态。

**真机运行验证中发现并修掉的三个问题**

跑起来之后立刻暴露了两个只看代码和测试发现不了的问题，都出在这一版新加的功能上。

- **同步灯带被误判为存在。** 开发机上服务端每次都推 `HidLightbar_Sync/Status`，但载荷是**整个状态结构的全空实例**：所有字符串字段 `null`、所有布尔 `false`。原先的判据是「这条主题到过就算这条灯带存在」，于是界面上会多出一个背后没有硬件的同步灯带开关和灯效入口。
  改判据的过程走了两步弯路，值得记下来：先加了「载荷得有可识别内容」，把 `effect` / `light` 也算作内容——错的，那两个字段是从保存的设置回显出来的，没有硬件也有值；又保留了「带了子灯带能力位就算有内容」——也是错的，GCU 用同一个状态结构发全部灯带主题，那几个能力位是**设备级**属性，出现在每一条载荷里（包括不存在的那条），毫无区分度。最终只认 `type`（SKU 标识符，官方也是拿它决定显示哪套灯带界面）与 `powerStatus`（真实电源态）。能力位取值为 `true` 仍然算证据，那是另一条通路，不受影响。
  验证：修好后铰链灯带仍然正确识别（这台机器上它是真实存在的，`type=MEZone_Lighbar4`），同步灯带判为不存在。
- **电池设计容量显示为 0。** 服务端报的是 `"0 mWh"` —— 字段非空但数值是零，照原样展示等于告诉用户「这块电池容量是 0」。现在只有解析出真正的正数才显示；循环次数（实测 15 次）不受影响。
- **功耗墙结论标签在「判不出来」时零宽。** 这个是 UI 审计抓的：AutoSize 标签空文本时宽度是 0，16 个视口全部报 `zero-size`。改为无文本时隐藏整个标签。

顺带补了一条一直缺的诊断信息：**运行时能力判定汇总**。日志里原本只有静态画像（注册表能力位），但界面显示什么取决于「静态位 + 服务端报过的字段」合并后的结论。缺这一行，排查「为什么这台机器上没有某个入口」只能靠猜——上面那个同步灯带的问题就是靠新加的这行日志定位的。变化才记录，不刷屏。同时，灯带状态在判为「无硬件证据」时会把整个原始载荷打出来，因为那正是判断某条子灯带是否存在时唯一的依据。

**验证结果**

Release 构建 0 warning 0 error；959 个测试全部通过、0 跳过（含对真实 GCU broker 的 MQTT 端到端连接与遥测测试）；UI 审计 16 个视口 690 张截图 0 问题（宿主 DPI 168）。

测试数从 966 降到 959，是撤掉那批「第三颗风扇字段拼写容错」测试的结果：它们断言的是一组不存在的字段能被解析，前提本身是错的，已换成一条锁定「这套协议里没有第三颗风扇」的测试。

真机运行验证：连接正常、遥测有效、59 行日志无异常（唯一的失败是未提权时显卡超频返回 `NVAPI_INVALID_USER_PRIVILEGE`，属预期）。四条灯带通道的判定结果为「灯条 / Logo / 铰链存在，同步不存在」，与硬件实际一致。

截图数从上一版的 638 涨到 690，是因为灯效审计新增了铰链与同步两个窗体。

审计本身也抓出了一个这一版引入的缺陷：功耗墙结论标签在「判不出来」时文本为空，而 AutoSize 标签空文本时宽度是 0——一个可见却零宽的控件。已改为无文本时隐藏整个标签，重跑后归零。

## beta9.7（2026-08-26）

本次没有新功能。前半部分是继承自 g-helper 的死代码清理，后半部分是把程序在真机上实际跑起来、逐行读运行日志之后发现并修掉的问题。

**真机运行验证中发现并修复的问题**

- 修复在没有 miniled 面板的机器上显示「多区背光」按钮的问题。按钮可见、可点，但点击后什么都不会发生。根因是 `AsusACPI.DeviceGet` 对没有数据来源的 ASUS 设备码返回 `0` 而不是 `-1`，继承代码普遍用 `>= 0` 判断「该硬件存在」，于是 `Settings.VisualiseScreen` 里的 `miniled1 >= 0` 成立。同一个根因还有三处后果：开启 HDR 后会冒出同样无效的 HDR 控制按钮；`ScreenControl.InitScreen` 把伪造的 miniled/overdrive 值写进 `config.json`；`Modes.InitFullSpeed` 每次启动打一行无意义的 `VivoBookMode` 日志。现在未映射的设备码统一返回「不支持」。
- 修复厂商 MQTT 端口暴露只有警告、没有处置手段的问题。GCUBridge 的 broker 监听在 `0.0.0.0:13688` 和 `[::]:13688`（不是仅本机），凭据是固定写死的，同网络的其他主机可以下发风扇曲线、功耗墙、显卡模式。程序原本能检测到缺少防火墙阻断规则，但只往日志里写一行英文警告——日志没人看，等于没有处置手段。现在补上 `--secure-mqtt` / `--secure-mqtt-remove` 两个命令行入口（需管理员）实际创建与撤销入站阻断规则，规则只拦入站、不影响本机连接，创建后会回读校验；日志警告也改成中文并写明具体该执行什么。
- 修复启动时上报一次并不存在的「电源切换」。`currentSource` 硬编码初始值为电池，插着电的机器第一次自动设置必然算出「电源已切换」，白白多跑一轮模式命令与 Windows 电源方案写入。实测启动 7 秒内触发了 3 轮 `SwitchCustomProfile` + 3 轮 `WinPowerPlan ApplyProfile`。现在未观测过时只建立基线、不算变化，同样的启动过程只保留 1 轮必要的施加。
- 修正一条会误导排查的日志。「Power source changed while custom mode is active」在由启动/唤醒/灯效重连触发时也照样打印，而这正是用户发过来排查的那份日志。现在文案区分真实电源切换与启动/唤醒保持。
- HID 枚举正常结束不再记为失败。`ERROR_NO_MORE_ITEMS`（259）是枚举走到末尾的正常返回，此前每次都会写一行 `SetupDiEnumDeviceInterfaces(57) fail err=259`，排查 HID 问题时容易被它带偏。
- 周期性状态回显改为「变化才记录」。液冷状态行内容完全相同、每 6 秒重复一次，一小时约 600 行；键盘灯、灯带、风扇表、超频支持位同样重复。日志上限 2 MB 且截断保留尾部，这类刷屏会把启动过程、异常、确认失败挤出去。改动后实测：一次 90 秒的运行里液冷状态 1 行、风扇表 1 行、超频支持位 1 行，整份日志 56 行且空闲时不再增长；每次新连接会重置基线，保证每一代连接都留下完整状态。
- 修复测试运行污染用户生产日志。`Logger` 是静态类、直写 `%AppData%\MechrevoLite\log.txt`，测试里用假对象驱动模式切换、显卡切换、液冷灯效的代码同样走它，实测一次全量测试往生产日志灌进约 900 行合成数据（`SwitchGpuMode(0) superseded`、`WinPowerPlan SetActivePlan rejected invalid GUID: not-a-power-plan` 之类），还会触发日志截断，把用户真实的排查记录挤掉。现在日志目录按三级优先级解析：`LMECHREVO_LOG_DIR` 环境变量 → 检测到测试宿主则落到测试输出目录的 `test-logs\` → 正常运行仍写 `%AppData%`。
  中间试过只靠环境变量（runsettings + 测试程序集的模块初始化器），但 `appPath` 是 `static readonly`、第一次触碰 `Logger` 就固化，与「谁先设好环境变量」存在竞争——实测同一份代码连续两次运行，一次隔离成功、一次仍然写进了生产日志。所以加了不依赖时序的测试宿主检测作为确定性兜底。改动后连续三次全量测试均未创建生产日志；同时验证了正常启动 `L-Mechrevo.exe` 的日志仍然落在 `%AppData%\MechrevoLite\log.txt`，守卫没有误伤真实用户。
- 让 `HARDWARE_DIAGNOSTICS` 编译开关真正生效。进程内硬件自测（selftest / rgbtest / lctest / customtest / colortest）的禁用守卫此前无条件 return，位置在 `#if HARDWARE_DIAGNOSTICS` 之前，导致那一整段自测代码即便定义了该常量也永远不可达。守卫现在放在取反条件里，默认行为不变，但开关不再形同虚设。

**经真机验证确认不是缺陷的两项**（记录下来避免以后重复排查）

- 启动日志里的 `Internal screen off`：出现在 `Monitor Power On` 电源状态切换的瞬间，Windows 尚未重新发布活动显示路径。稳态下内置面板能正确识别为 `DISPLAYPORT_EMBEDDED`，同一次运行里 `frequency=240`、`max_frequency=300` 都写入了真实值。
- `RGB heartbeat: 1801 帧/60s`：当前效果是 HID 模式 1（流畅彩虹），动画效果按 30 fps 发帧属正常设计。顺带记录一个待办：静态效果（模式 9）走的是同一个帧循环，会以 30 fps 重复发送完全相同的帧。这一项没有改动——改动灯效发帧节奏需要肉眼确认键盘不会熄灭或闪烁，本次无法验证，不做没把握的改动。
- GPU 显存偏移：硬件带着 +1624 MHz（nvidia-smi 显示 15625 vs 规格 14001），而 `config.json` 里存的档位值是 0，普通权限下无法写回。界面显示的是 `EffectiveGpuMemoryClockOffset`，它走 NVAPI 实读，因此呈现的是真实的 1624 而不是配置里的 0，确认状态没有错配。仍有一个待改进点：恢复档位失败目前只有日志（`memory=0/False`），界面上没有可见提示。

**产物瘦身：移除随包但永不执行的负载**

- 删除 `Pawn/IntelMSR.bin`。它以嵌入资源形式打进单文件产物，但代码里**没有任何加载点**，是 5,292 字节的纯死负载。AMD 的 `RyzenSMU.bin` 保留（它有真实加载路径），并在 csproj 里写明「加新 .bin 前先确认存在对应的 Load 调用」。
- 删除 `Helpers/AudioVisualizer.cs`。它的 `Shared` 单例在整个代码库里零订阅者，配套的 `Settings.VisualiseAudio` 也没有调用者——是 g-helper 的 Slash/AnimeMatrix 音频律动灯效残留。随之移除 `FftSharp` 依赖。`NAudio.Wasapi` 保留，麦克风静音开关（`Helpers/Audio.cs`）需要它。
- 删除 `Display/ColorProfileHelper.cs`（693 行）。它是一张约 640 条的「ASUS 机型 → ICC 配置包」映射表，基址是 Base64 编码的 ASUS CDN 地址。查表键取自 BIOS 版本串，机械革命的格式（例如 `N.1.32MRO60`）永远匹配不到任何 ASUS 机型，所以「下载色彩配置」按钮从未出现过。按钮本体也一并从设计器移除。本机色彩校正走的是另一条路（`ColorCalibrationForm` + `MechrevoService.SetColorCalibration`），不受影响。
- 从 `AppConfig` 移除 48 个传递不可达的 ASUS 机型判定方法（`IsStrix`、`IsG14AMD`、`IsTUF`、`IsProArt`、`IsZ13`、`IsAnimeMatrix`、`IsLampArray`、`IsROG`、`NoWMI` 等）。判定依据是从「代码库其余部分 + 测试」出发做的传递可达性分析，不是逐个肉眼看。文件从 698 行降到 500 行，布尔方法从 90 个降到 42 个。

**华硕 Splendid 通路改为显式门禁**

- `Display/VisualControl.cs` 的色域与视觉模式此前是靠「ASUS 的 ATK 驱动和 `%ProgramData%\ASUS` 目录恰好不存在」层层空转来失效的——不是显式判定。代价是每次启动和每次视觉设置都要跑一遍 WMI 驱动查询，`SetVisual` 还会额外触发 HDR 状态与刷新率探测。现在改为一次性平台能力判定（`AsusSplendid.exe` 是否真的存在），非 ASUS 平台在所有入口直接短路。

**BIOS 版本解析**

- 修复非 ASUS 机型上 BIOS 版本被解析成假值的问题。旧逻辑套用 ASUS 的「机型代号.版本号」约定，按 `.` 切分后取两段，机械革命的 `N.1.32MRO60` 会得到机型 `N`、版本 `1` —— 两个都是错的。现在只在确实符合该格式时才拆分，否则整串原样作为 BIOS 版本返回、机型留空。调用方拿到的要么是真值要么是空，不会是似真而假的值。

**本地功耗监控的驱动重试**

- `LhmMonitor` 的重开尝试加了上限。它每次尝试都会去注册 LibreHardwareMonitor 的内核驱动服务，非管理员运行时必然失败，而此前只有 3 秒节流、没有次数上限——悬浮窗常开就等于每 3 秒尝试注册一次内核驱动，且日志被刷屏。现在改为指数退避（3/6/12/24/48 秒）后彻底放弃，并给出一次说明性日志。

**UI 审计可复现性**

- 消除了 UI 审计此前唯一已知的验证盲区。审计模拟目标视口 DPI 的做法是「在宿主 DPI 下正常布局，再用 `form.Scale(视口DPI / 宿主DPI)` 相对缩放整棵控件树」。问题出在绘制环节：`Control.Scale` 不会改变 `Graphics` 上报的 DPI，而 RButton / RCheckBox / RColorButton / RBadgeButton / RComboBox 的圆角半径与描边粗细都直接读 `Graphics.DpiX`，于是截图里「控件尺寸已按视口 DPI 缩小、圆角却仍按宿主 DPI 绘制」，同一份代码在 96 DPI 与 168 DPI 主机上跑出不同结果。
- 新增 `UI/UiDpi`，把 DPI 读取明确分成两套：`Layout` 始终返回真实宿主 DPI（参与 `form.Scale` 之前的尺寸计算），`Paint` 在审计期间固定为视口 DPI（只影响像素怎么画）。两者混用会把同一缩放比例乘两遍——实现过程中确实踩到过，一次性把审计问题数从 0 推到 731 个 text-clipping / parent-overflow 误报，已由测试锁死这条不变式。
- 审计报告新增 `HostDpi` 字段。窗体布局仍会经过一次相对缩放，记录宿主 DPI 便于判断两次审计的差异是否来自运行环境而非代码。

**测试**

- 新增 97 个测试（665 → 762）。补齐了此前零覆盖的 `AppConfig` 存储层：配置位置优先级（便携 / SYSTEM 的 ProgramData 副本 / 用户 AppData）、加载降级链（不可用内容识别、损坏文件的正则抢救、`.bak` 兜底）、原子写与 `.bak` 世代、ProgramData 副本同步的前置条件。另外覆盖了 BIOS 解析、LHM 退避、`UiDpi` 的布局/绘制分离不变式，以及本次真机排查出的每一个缺陷（未映射设备码必须报告不支持、首次电源观测只建立基线、状态日志去重语义、防火墙规则名与端口匹配、日志目录三级解析与测试宿主识别）。
- 每条回归测试都注明了当时的实测证据，避免以后有人「优化」回旧行为。
- 验证结果：Release 构建 0 warning 0 error；762 个测试全部通过、0 跳过（含对真实 GCU broker 的 MQTT 端到端连接与遥测测试，实测 145 ms 内连上并收到有效 CPU 温度）；UI 审计 16 个视口 638 张截图 0 问题。截图数比上一版少 2 张是修复带来的：`Settings-Devices` 页在 3840×2160@200% 下原先内容过高需要 3 张分页，移除幽灵按钮后一张就装得下。

**文档**

- `Pawn/PawnIOWrapper.cs` 加上现状说明：这条内核驱动通路默认休眠，需要 `auto_uv` 配置为真（无 UI 入口）、CPU 为可识别的 AMD 型号、且机器上装有 PawnIO 驱动，三者同时满足才会加载；本项目不分发也不代装该驱动。文中同时写明启用前必须补齐的三件事。
- `THIRD_PARTY_NOTICES.txt` 补上 watercooler-manager 的出处说明。液冷 BLE 支持是独立的 C# 实现，但 GATT 标识与命令布局参考自该 Python 项目（GPL-3.0），此前未致谢。

## beta9.6（2026-08-25）

**显卡热切换**

- 支持热切换的机型现在可以从混合/标准模式免重启切到核显。能力判定完全依据官方注册表字段（`IsNvGpu`、`iGPUModeOnlySupport`、`APVersionCheck > 23`、`GpuHotSwapSwitchSupport`、`lgpuHotSwapSwitchStatus`），任一字段缺失、格式异常或互相冲突都按不支持处理，不使用机型名或显卡代次做条件判断。
- 显卡区新增「自动模式离电切核显」和「允许关闭独显程序」两个开关，仅在机型确实支持热切换时出现；开启强制关闭独显程序需要额外确认，取消会还原开关与配置。
- 切换路线改由实时 GCU 状态解析（当前模式、自动模式运行态、目标模式、热切换能力），不再从可能过期的本地 `gpu_mode` 推断。需要重启时使用厂商的 `DGPU_DIRECT_CONNECT_RESTART`，移除自行调用 `shutdown` 的路径。
- 独显占用程序的枚举与关闭移入独立协调器：排除当前进程、会话 0 进程、无权访问进程和关键系统进程；先请求优雅关闭并复查剩余列表，强制关闭前重新校验 PID、进程名与会话身份。
- 热切换未被 GCU 确认、或 GCU 报告独显仍被占用时会明确提示并提供重启切换，不再把未落地的目标显示成已生效；取消、超时和失败路径都会刷新真实硬件状态。
- 仅有 MUX 的机型继续显示核显直连与独显直连，但不再显示依赖核显直通能力的自动模式。

**液冷泵与风扇**

- 水冷泵速和风扇档位改为等设备回报的新状态与请求一致后才算成功。命令后没有新状态时主动补发一次 `GETSTATUS`；先到的旧状态包会再查一次以排除在途回包。整个确认过程只重试状态查询，绝不重复下发控制命令。
- 设备拒绝或没有回报时界面回滚到上一个档位；GCU 通道的成功文案由「命令已发送」改为「已确认」，直连蓝牙文案不变。

**键盘灯与灯效恢复**

- 键盘亮度回读优先使用官方 `brightNess` 百分比，`0..4` 仍按五档换算，避免亮度显示与实际不一致。
- 灯效恢复按连接代次去重：同一次连接不会重复恢复，进行中的重复请求排队等待首次尝试结束，失败后仍可重试，强制恢复可跳过已完成标记。
- 电池临时熄灯与用户主动关灯改为分开跟踪，来电或唤醒后只恢复用户想要点亮的情况；恢复失败时保留临时标记以便下次重试。
- 键盘关灯状态、睡眠时间和自定义速度跨重启保持；HID 模式下保留用户睡眠时间同时禁用 EC 定时；仅在当前渲染器无法复用时才重启灯效。

**显卡超频提权**

- 提权写入助手的命名管道加入仅当前用户令牌可访问的 ACL。
- 提权被拒绝后进入冷却期，不再反复弹出 UAC。
- 普通权限回读陈旧时改用提权回读确认核心/显存偏移；GCU 的状态回显不会被当作驱动写入成功。

**安全边界**

- 不再从普通用户可写的目录注册以 SYSTEM + 最高权限运行的开机充电任务。该任务执行的是磁盘上的镜像，若目录可写，替换 EXE 即可在下次开机获得 SYSTEM 权限且持久化。新增镜像位置信任校验（目录与文件 ACL 都必须只有管理员/SYSTEM 可写，读取失败按不受信任处理）；便携解压运行时跳过注册并写明原因，登录后的用户级自启动任务仍会应用充电限制。
- 官方控制台的「恢复」不再无条件信任磁盘上的状态文件。此前恢复流程会按快照内容做管理员级的注册表写入与文件移动且零校验，可写 HKLM 任意键。现在恢复前逐项白名单校验：注册表项必须落在采集时扫过的 Run/RunOnce、值类型限字符串、值名不含分隔符；启动目录文件必须是「我们自己改名的那一个」（原路径位于启动目录的直接子项，禁用路径正好是原路径加固定后缀）；计划任务排除 `\Microsoft\` 系统目录。快照目录改为显式设置仅管理员可写的 ACL。
- 提权 GPU 写入助手现在校验连接方身份：管道 ACL 只授予预期的客户端 SID 与 LocalSystem，服务端再用客户端令牌核对一次 SID，请求还需携带父进程生成的一次性凭据。助手空闲存活上限由 20 分钟收紧到 5 分钟，发送失败时主动结束助手进程，UI 退出时一并回收，不再留下带可写超频管道的孤儿提权进程。响应写完后等待管道排空，修复偶发的「助手没有在限定时间内响应」。UAC 被用户取消与真实启动失败现在区分处理，不再把启动失败误报为「已取消授权」。

**状态正确性**

- 统一了 GCU 报文的解析语义。此前同一份代码里有四套互不一致的默认值规则，最严重的后果是「未知」被伪装成一个看起来合理的真实档位：无法解析的 `OperatingMode` 会变成办公档、`HealthProtectionStatus` 会变成满充、功耗会变成 0 W，并绕过所有有效性守卫。现在无法解析一律视为未知。
- 布尔字段改为统一的确定性解析，能读懂 JSON 布尔、`0`/`1`、以及 `ON`/`OFF`、`SUPPORT`/`NOT_SUPPORT` 这类枚举字符串，且用精确匹配避免 `NOT_SUPPORT` 被当成支持。此前有五处字段用的解析方式遇到字符串数值会抛异常，其中 `IsAC` 位于 `Fan/Status` 的开头——一旦固件把它写成 `"1"`，整帧的模式、功耗墙、温度墙、TGP、超频回读会全部停留在旧值。
- 显卡切换能力不再由「状态字段非空」推断。此前 `NOT_SUPPORT`、`UNKNOWN`、`NONE` 这类值都会被当作支持，还会覆盖正确的机型画像，导致 UI 暴露本机不具备的切换入口。现在只有能被解析成具体模式的状态才算证据，并新增了防漂移测试保证识别表与模式解析表不会各自演化。
- 修复 Intel 机型被误判为 AMD 功耗字段体系的问题。此前只要报文里出现 `CPU_AmdSPL` 这个键（哪怕值是 0）就会永久切到 AMD 通道，功耗墙写入被 GCU 忽略、确认必然超时，同时功耗显示被锁在 0 W。现在平台判定要求字段携带可用瓦数。
- 风扇增压、独立风扇控制、自动刷新率这几项能力，现在只有在真的解析出布尔值时才认为「机型支持」，不再出现「显示支持但状态永远关闭」。
- 状态版本号改为在字段全部写完之后才自增。此前它在解析开头就自增，若解析中途失败，按版本号轮询的代码会认为收到了新状态而读到的全是旧值，表现为「明明已生效却确认失败」的偶发误判。
- 风扇曲线的默认值回退改为在发布前完成，不再原地改写已发布的数组，消除界面偶发画出半截错误曲线的可能。曲线点越界改为范围钳制而不是字节截断。

**可靠性**

- 解析失败现在会写入日志。此前这条日志用的是仅在调试构建中生效的写法，Release 版本里整行被编译移除，导致这类故障对用户表现为「点了没反应」、对开发者表现为「日志里什么都没有」。
- 显卡热切换的确认最长约两分钟，此前全程占用与性能模式、自定义档共用的同一把切换锁，用户在此期间点性能模式最长两分钟没有任何反应。现在等锁超过阈值会请求长操作让位，由界面重新回读真实状态。
- 液冷的所有命令通道（泵速、风扇档位、灯效序列、连接、状态查询）现在共用同一把锁。此前只有泵速和风扇的确认路径加锁，灯效档位的多条命令序列可能交叉下发让设备停在混合档位，别人的状态查询也会占掉泵速确认唯一一次的补查机会，把本该成功的写入判成失败。
- 模式切换的过期包过滤窗口改为原子读写，并在命令发送失败时立即解除。此前连点两个模式按钮可能让真实到达的新模式被当作过期包丢弃；命令发送失败时窗口仍然生效，接下来 8 秒内一切真实模式变化（包括用户按厂商快捷键）都被静默丢弃，界面显示一个从未生效的模式。
- 退出时不再在控制命令仍在进行时强行释放内部同步对象，改为有界等待；断开连接也加了超时，避免后台服务无响应时退出流程挂住。
- 直连超频的可调范围读取改为在锁内一次性取快照，消除界面读取属性时的偶发空引用。
- 状态通知改为逐个订阅者隔离调用。此前一个订阅者抛异常会掐断后续的整条通知链，导致所有等待确认的操作只能超时、界面停止刷新，而异常本身被静默吞掉。
- 等待状态确认的辅助方法不再把判定条件里的异常和取消抛给调用方——这些方法的语义是返回成功/失败，调用方没有为异常做准备。
- 发布命令失败现在抛专用异常类型并带主题名，调用方可以按类型区分「命令没发出去」和其他程序错误；同时加了发布超时上限。
- 校色开关的注册表回读加了短时缓存，不再在每帧状态推送时于接收线程上做阻塞式注册表读取；确认路径会主动绕过缓存以保证准确性。

**官方控制台隔离**

- 隔离状态文件读取失败时不再继续。此前读取失败与「文件不存在」无法区分，隔离流程会在启动项已被删除的情况下采集到空快照并覆盖唯一一份原始记录，用户的原状态永久丢失，之后「恢复」还会报成功。
- 恢复改为逐项容错并汇总结果：一项失败不再中断整批；实际恢复项数为 0 或存在失败项时明确报告失败并保持隔离状态，用户可以重复点击恢复，不再出现「报告已恢复但什么都没做」。
- 注册表项只在值确实缺失时才写回，避免复活已被系统消费的一次性启动项，也不会覆盖用户在隔离期间自己改过的值。
- 识别标记收紧为要求可执行文件名或包族名，不再用裸名匹配；计划任务扫描排除系统目录；启动目录只对快捷方式和脚本扫描内容并限制文件大小，不再整体解码任意可执行文件。
- 终结官方界面进程限定在当前登录会话内，不再连带结束其子进程（可能是官方的更新程序或安装器），并且只统计确认已退出的进程。

**开机任务**

- 用户级自启动任务的自动修复不再连带删除需要管理员权限才能重建的 SYSTEM 充电任务。此前非提权运行下的一次自动修复会让充电任务静默消失，直到用户下次以管理员身份运行。
- 修复了当前账户读不到 SID 时初始化失败、进而导致整个自启动模块所有功能不可用的问题。

**构建**

- 排除 `src/Probe/artifacts/**` 的编译项发现，修复全解决方案 Release 构建的重复程序集特性错误。
- 开启完整的可空性检查。此前代码里到处使用可空标注但编译器不做任何检查，上面那个读不到 SID 的空引用正是因此无声通过编译的。硬件协议层、Helpers、GPU 三个核心目录已全部清零；少数继承自上游的界面与原生互操作文件在文件头显式标注为待迁移，并有测试守卫防止豁免范围扩大或配置被改回。
- 测试从 394 个增加到 654 个，覆盖上述每一项修复。

## beta9.5（2026-08-22）

- 修复自定义风扇曲线在最高有效温度点被客户端强制锁定为 100% 的问题。现在在设备允许的范围内保留用户设置，并以同一占空比填充温度哨兵后的协议槽位，避免尾部默认值覆盖最后一个有效档位。
- 保留 beta9 的开机状态同步、悬浮监控遥测和 GPU 超频实际回读修复。

## beta8.3（2026-08-19）

- 修复睡眠唤醒后键盘灯持续闪烁、重启后灯效未恢复和重复写入问题。
- 修复 2023 款 16S 校色状态读取、悬浮监控重启后不显示和显卡功耗为空问题。
- 修复平衡模式偶发显示为静音，以及自定义档启动后被恢复为平衡的问题。
- 自定义档切换后强制刷新当前档参数，AMD 9955HX 功耗和 40/50 系 GCU 超频能力识别更完整。
- 扩展显卡模式与校色状态字段兼容，并加入官方控制台协议取证资料。

## beta8.2（2026-08-18）

- 修复 RTX 5080 核心超频超过 245MHz 后被错误恢复的问题，按 GCU 实际状态确认更高偏移。
- 修复蛟龙 16 Pro 9955HX 自定义功率无效，改用 AMD 官方功耗通道并确认回读。
- 保留显卡驱动回读校验，未真正生效的参数不会显示为成功。

## beta8.1（2026-08-18）

- 修复退出后重新打开控制台反复请求管理员权限的问题。
- 开机和性能模式初始化不再自动触发提权；仅用户明确执行需要权限的操作时请求一次。
- 旧的开机任务和电池保护任务不会在后台反复修复或弹窗，手动重新启用开机启动时仍可正常更新。

## beta8（2026-08-18）

- 自定义模式的 GPU 核心/显存频率现在由独立提权写入助手执行，并以 NVIDIA 驱动的实际回读值确认结果；写入未落地时会保留真实数值并提示失败，不再把 GCU 回显当作物理写入成功。
- 核心频率保持 `-500～+500 MHz` 的用户范围；显存频率不再被固定 500 MHz 上限截断，界面会按当前显卡驱动报告的真实可调范围显示。
- 修复重新打开自定义模式后 GPU 超频数值显示为 `0/0`、高性能电源开关陈旧状态误判、熄屏/唤醒后性能模式被旧状态回退，以及后台遥测偶发停滞的问题。
- 灯效调整改为短暂合并后再发送，降低流畅彩虹、灯条速度和颜色滑条操作期间的重复命令与可见闪烁；键盘关闭状态、速度和用户设置继续持久化恢复。
- 自定义模式的功耗、频率、温度与灯效参数补充精确数值输入；悬浮窗增加 CPU 功耗显示。
- 改进核显/自动显卡模式的能力识别和切换重试；不会自动禁用虚拟显示设备，避免影响远程桌面、投屏和虚拟机环境。

---

## beta7.6.2（2026-08-16）

- 修复 NVIDIA 直连写权限不足后自定义模式 GPU 核心频率范围从 `±500 MHz` 缩回固件声明的 `±250 MHz`；已探测到的真实硬件范围现在与写权限状态分离，界面和参数校验会稳定保留 `-500～+500 MHz`。
- 对 GCU 声明上限为 250、但实际能够接受更高偏移的机型，超过 250 的参数会在驱动路径不可用时继续交由 GCU 写入并按回读值确认；仍严格拒绝超过 `±500 MHz` 的请求。
- 修复“高性能电源”显示关闭但 Windows 实际仍保持高性能的问题；关闭时同时停止本程序后续的自动电源模式映射，并将活动电源方案及覆盖模式恢复为平衡。
- 高性能电源开关改为 GCU 状态与 Windows 实际方案双重确认；开启后按办公/平衡/增强/自定义模式分别映射最佳能效/平衡/最佳性能/平衡，任一层未落地都不会显示成功。

---

## beta7.6.1（2026-08-16）

- 修复自定义模式 GPU 核心/显存频率无论怎样调整都提示“参数未应用，已恢复回读值”的问题。根因是 NVIDIA 驱动允许普通权限读取超频范围、却拒绝写入时，程序仍错误地绕过了可用的 GCU 通道。
- GPU 超频改为在设备原生范围内优先使用 GCU，超出固件范围时才尝试 NVIDIA 驱动；驱动权限不足或写入失败会自动回退 GCU，不再让后续每次拖动重复失败。
- NVIDIA 驱动写入增加短时多次回读，兼容移动显卡延迟生效；确认权限拒绝后会停用本次运行中的直连后端，并恢复使用设备实际可用范围。
- 修正 GCU 与驱动后端同时可见时的状态来源，GCU 写入后的核心/显存频率与超频开关现在按 GCU 回读值确认和显示。

---

## beta7.6（2026-08-16）

- 修复耀世 5080 等新机型把 `0/0` 温度偏移范围误判成不可调，导致自定义模式温度墙消失的问题；旧机型只提供单侧范围时继续按真实限制显示。
- 自定义页面的 NVIDIA 驱动超频探测改为后台执行并在每次打开时重试，避免独显尚未唤醒时核心/显存频率入口永久不可用；实际范围支持 `±500 MHz` 时可完整调到端点，仍拒绝超出驱动范围的值。
- 风扇独立控制同时识别 `Fan/Table` 与 `Fan/Status`，修复部分耀世机型开关无法点击或刷新后消失；模式切换扩大迟到旧状态的屏蔽窗口，减少性能模式被旧回包拉回平衡。
- 新增水冷泵和风扇自动档，根据 CPU/GPU 较高温度自动选择档位；手动/自动档、灯效和上次设备均会保存，重启连接后自动恢复。
- 水冷优先直连上次设备，保持 BLE 会话并在物理断线后限次重连，改善开机概率断联；重复温度档不重复发送命令。
- 修复屏幕亮度由设备状态刷新时百分比不随滑条变化；官方键盘效果在固件启动回到默认值时重新保存用户效果、亮度和速度。
- Aurora/极光系列 ITE8291 枚举兼容只允许写权限的 HID 接口，并增加候选接口诊断；彩虹轮中心调整到 `K` 键。
- 苍龙高性能电源开关增加失败重发和大小写兼容回读；托盘一级菜单明确显示当前自定义档并高亮。
- 悬浮窗在绘制前一次性确定最终尺寸，修复旧尺寸位图触发嵌套重绘造成的首帧闪动和宽度跳变。

---

## beta7.5（2026-08-16）

- 修复部分旷世机型打开或使用控制台时容易无响应的问题：官方控制台安装状态、开机启动状态等系统查询改为后台执行，官方目录扫描加入缓存，并移除主界面启动路径上的无效 WMI 机型探测。
- 修复显卡模式切换后没有重启提示的问题：涉及独显直连的进入、退出及状态不同步场景，均在确认切换成功后提示重启；暂不重启时持续显示待重启状态。
- GCU 未连接或当前机型不支持目标显卡模式时，现在会明确提示，不再表现为点击无反应。

---

## beta7.4（2026-08-16）

- 修复开机登录任务早于 GCU/MQTT 服务启动时，首次连接异常后不再重试的问题；连接拒绝、订阅失败和初始化失败现在都会进入有上限退避的自动重连。
- 首次连接及后续重连统一执行完整恢复：重新请求设备状态、风扇曲线、电池档位和 `System_ON`；800ms 内没有温度遥测时自动补发，托盘启动不再长期显示空温度。
- 键盘灯配置在设备连接前从磁盘加载，HID 与官方灯效分别等待设备就绪并持续重试；重连后重新应用用户保存的效果、亮度、速度和睡眠时间，不再回到固件默认速度。
- 硬件恢复按连接代次串行去重，重连完成后同步刷新机型能力、传感器、显卡模式与当前自定义超频设置，避免重复初始化互相覆盖。

---

## beta7.3（2026-08-16）

- 自定义模式 GPU 核心频率偏移上限提升至 `-500~+500 MHz`。
- 新增 NVIDIA NVAPI 驱动直连后备通道：GCU 超频范围不足或通道不可用时，读取驱动实际可调范围并直接应用核心/显存偏移。
- 驱动直连写入后立即回读确认；超出驱动范围的值不会下发，关闭超频会恢复核心与显存偏移为 0。
- 驱动直连设置按 4 个自定义档分别保存，切换到非自定义模式时暂停，重新进入对应档位时恢复。
- 保留 GCU 兼容路径；无 NVIDIA 可编辑 P-State 的机型不会启用 NVAPI 后备通道。

---

## beta7.2（2026-08-16）

- 自定义模式 GPU 核心频率偏移增加 `-400~+400 MHz` 用户范围；实际可调端点继续与当前 GCU/固件上报范围取交集，窄范围机型不会发送越界值。
- 扩展官方 ItemSupport 字段别名，并记录系统厂商、产品名、系列、SKU、主板与 BIOS 组成的机型指纹，改善不同代际控制台和旧机型的识别与问题定位。
- 修复 Logo 灯效入口消失：增加官方 Logo 灯注册表布局识别、独立 `HidLightbar_Logo/#` 状态订阅和启动/刷新状态请求。

---

## beta7.1（2026-08-16）

- 修复键盘灯睡眠时间虽然写入本地配置、却被设备启动状态覆盖的问题；开机自动恢复用户设置，官方模式与自定义模式分别使用合适的定时方式。
- 修复官方单色灯效在休眠、唤醒或重启后恢复成彩色波浪的问题；灯效停止调整后持久化一次，唤醒时重新恢复当前效果。
- 发布包新增简明更新日志和“用前必看”，打包时自动检查文档版本并使用当前版本号命名 EXE 与 ZIP。

---

## beta7（2026-08-15）

- 修复自定义模式 CPU 温度墙在部分机型无法调节：兼容 GCU 只上报温度墙上限、不提供下限的状态格式，并继续按 Intel/AMD 各自协议做范围校验与回读确认。
- 修复显存/核心频率偏移被服务层错误拒绝：GPU 超频改用允许负下限的频率区间校验；LCHWOC 未携带 `Support` 字段时，以设备实时上报范围和官方机型画像判定，明确上报不支持时仍保持禁用。
- 自定义模式现在会跨 AC/DC 电源变化保持：拔掉或插入电源后重新应用当前自定义档、Windows 电源计划与睿频设置，不再被旧的普通模式自动切换链覆盖。
- 修复 CPU 自定义风扇曲线重新打开后视觉恢复默认的问题：GCU 回读的平缓/平台曲线现在视为合法用户设置，仅对真正全 0 的未初始化表使用安全默认值。
- 开机启动改为静默进入托盘：计划任务统一携带 `startup` 参数，并自动修复旧版无参数任务；手动启动仍正常显示主界面。
- 重写自动显卡模式的电源切换确认：拔电后不仅检查“自动”配置，还确认硬件真正进入核显状态；官方自动命令未落地时自动回退到明确的核显/标准命令并重试。
- 核显与独显直连能力改为以 GCU 实时状态优先；不支持核显的机型不再显示核显/自动按钮，并自动清理旧版残留的自动配置。
- 点击自定义模式固定进入“自定义 1”；切换档位前保存待提交参数，修复温度墙的范围、数据类型、命令顺序和确认超时，避免保存报错或看似成功但未生效。
- HDR/自动色彩管理状态改为读取 Windows 当前内部屏幕的活动显示路径：Windows 11 优先使用精确的 SDR/WCG/HDR 模式，旧系统再走兼容读取；不再使用可能残留的厂商注册表值误报 HDR。
- “静音狂暴”只在官方能力字段明确支持时显示；不支持的机型不再发送相关命令，普通狂暴模式也不会误触发子模式切换。
- 修复从系统托盘恢复主界面时连续闪烁两次：隐藏状态下先完成 GPU 状态、DPI 布局、目标屏幕和置顶状态更新，再只执行一次原生窗口显示，不再在显示后强制重绘、二次移动或切换 Z 序。
- 托盘恢复保持窗口原来的显示器，不再先移动到主屏幕；UI 审计新增连续隐藏/恢复两次、句柄复用、可见性和不透明度断言，防止白屏与重复建窗回归。
- 性能模式切换改为等待 GCU 确认后再应用显卡频率、风扇和功耗；新模式会取消旧模式的延迟任务，避免旧命令晚到覆盖新选择。
- 显卡模式切换增加“最新请求优先”取消链，等待锁、状态轮询、协议重发和自动重启前均检查是否已被新请求替代。
- 修复亮度/滑条长期重绘时每帧泄漏 GDI 画刷的问题，并补齐悬浮监控画刷、画笔、字体、计时器与原生绘图句柄的释放。
- 补齐主窗口、托盘、BLE、灯效、日志、注册表、设备枚举和电源事件的资源生命周期；退出时不再遗留计时器、图标、注册表句柄或待执行回调。
- 原生 DLL 搜索限制为 Windows 安全目录，降低高权限运行时从当前目录误加载同名 DLL 的风险；GCU MQTT 端口继续由独立安装包防火墙规则限制远程入站。
- 修复异常长度风扇曲线可触发数组越界；新增 0/1/7/15/17 字节损坏配置与合法 16 字节曲线测试。
- 完成全项目 Release 编译、单元测试、全规则静态分析和多分辨率视觉矩阵复核；保留的旧 WPF 原型、未接入音频可视化器及 ASUS 兼容存根已记录在深度审计报告中，不进入当前发布路径。

---

## beta6（2026-08-15）

- 修复每次打开控制台都要重新设置键盘灯速度：官方灯效速度、亮度、效果和颜色现在参数变化后自动写入本地配置，不再依赖关闭灯效窗口或正常退出程序。
- 补齐 BetterRGB 自定义灯效的全部速度、密度、平滑、颜色和布局参数持久化；旧版 `rgb.cfg` 可直接兼容，配置改为合并写入与原子替换，避免异常退出或并发保存造成回退。
- 官方键盘灯连续调节改为 120ms 合并提交且仅执行最后一次命令，避免旧 MQTT 命令晚到覆盖新速度；新增键盘速度/方向状态诊断，并隔离会误写真实用户配置的测试路径。
- 修复手动切换核显不生效：核显、标准、自动、独显直连改为四个独立状态，分别使用官方 `IGPU_ONLY_CONNECT_RB_ON/OFF/AUTO` 与 MUX 直连协议，并联合两组 GCU 状态回读确认。
- 修复自动显卡模式拔电后不切核显：电源变化现在会重新向 GCU 下发官方自动模式，延长状态等待并周期重试；旧版 `gpu_mode=1 + gpu_auto=1` 配置会自动迁移。
- 核显/标准/自动热切换不再错误要求重启；仅进入或退出独显直连时重启，核显切换失败会提示先关闭正在使用独显的程序。
- 将“赞助 / 更新 / 退出”从系统页内容中抽离为主窗口底部常驻操作栏，切换常用、设备、系统页面时始终可用。
- 新增一次性首次使用教程：引导按机型或 SN 安装官方 50 系最新版控制台、在系统页隔离官方界面与托盘，并警告不要同时运行其他灯效控制软件抢占 HID 设备。
- 键盘灯效 HID 枚举完整对齐 BetterRGB：支持全部 `VID 048D` 下 `UsagePage FF03` 或 `MI_01` 的兼容 ITE8291 接口，`PID 600B` 改为优先级加分而非硬限制。
- 增强 40 系及更早机型能力识别：兼容 32/64 位注册表视图、旧控制台注册表根、历史字段别名和文本型开关；高级功能仅在官方画像或 GCU 实时状态确认支持后显示。
- 修复点击“屏幕校色”时窗口先在左上角闪现再跳到主窗口旁边：窗口现在先透明完成 DPI 布局和最终定位，再显示首帧。
- 重新压缩常用页间距并将首次教程纳入视觉矩阵；覆盖 1280×720 至 3840×2160、100%-200% 缩放的 16 组视口，228 次最终渲染审核问题归零。
- 取消每次启动时关于 GCU MQTT 端口 13688 的气泡警告；安全诊断仍写入日志，不再打扰正常使用。
- 官方控制台面板与隔离确认框新增“无需卸载官方控制台”提示，明确只需点击“隔离官方界面与托盘”。
- 修复自定义模式 GPU 超频无法点击：启动及每次打开页面都会主动刷新 LCHWOC 状态，并正确接受 `-200～+200 MHz` 等包含负值的设备频率偏移范围。
- 修复自定义模式电源计划选择后未保存、再次打开为空白：改为读取下拉框实际 GUID，按 4 个自定义档独立持久化，并在首次打开和切换档位时恢复所选计划与睿频模式。
- 新增可逆的“官方控制台隔离模式”：一键关闭并阻止 `CCUWinUI`、`SystrayComponent` 等官方界面/托盘启动，同时保留 `GCUBridge`、`GCUService`、`OSDTpDetect` 与 UWACPI 驱动。
- 隔离前自动快照官方 Run 启动项、计划任务和启动目录文件；恢复时按原状态回写，并重新启动官方控制台。
- 隔离启用期间增加官方 UI 进程守护，避免托盘组件被热键或官方程序重新拉起；终止后主动刷新通知区域，清除残留图标。
- 新增主界面“官方控制台”状态面板，在不同分辨率/DPI 下自适应显示隔离状态与 GCU 后台健康状态。
- 主界面客户区从 `760×640` 缩为 `680×350`，默认占用面积减少 51.1%；按用户频率重组为“常用 / 设备 / 系统”三页，并记忆上次页面。
- 高频模式与亮度留在首屏，液冷、灯效与刷新率归入设备页，官方控制台、更新和退出归入系统页；窗口尺寸切页时保持稳定，窄屏页面独立滚动。
- 屏幕亮度改为 120ms 合并提交，并启用父子控件裁剪与双缓冲，修复拖动亮度时性能模式、显卡模式按钮闪烁。
- 从托盘恢复时先以透明状态完成深色布局、整窗重绘和最终定位，再显示不透明首帧，修复短暂白色窗口。
- 修复官方界面已隔离、GCU 正常连接时仍显示“后台未运行”：改用 MQTT 连接与系统进程双重健康信号，并每 3 秒自动刷新。
- 修复高 DPI 下“深度睡眠”和时长下拉框可能被拆到两行的问题；覆盖 1280×720 到 3840×2160、100%-200% 缩放的 16 组视口，197 次最终渲染审核问题归零。
- 按官方控制台机制读取 `ItemSupport` 机型画像，并与 GCU 实时状态合并；无液冷、无灯条、无校色、无 MUX 等机型会自动隐藏对应入口并重新排版。
- CPU PL1/PL2、GPU TGP/动态加速和超频范围全部改用当前设备下发值；140W、175W 及固定功耗机型不再套用通用上限，越界或不支持命令会在发送前拒绝。
- 修正温度墙协议：Intel 界面目标温度自动换算为 `TjMax - target`，AMD 改用官方 `CpuAmdTccTarget` 字段，并按各自范围显示。
- 新增机型能力矩阵测试，覆盖 140W/175W、固定 TGP、无独显直连、无超频、过期注册表画像以及 Intel/AMD 温度墙差异。
- 修复液冷风扇把百分比错误换算为 `0-255`，导致 40/50/60/90% 全部越界、升档后无法降档的问题。
- 修复液冷泵始终发送 100% 占空比的问题；四档现在同时设置 45/60/90/100% duty 与对应 7/8/11/12V 电压。
- 补齐 LCT21001/LCT22002 头灯的静态、呼吸、多彩、彩色呼吸模式，以及 LCT22002 独有的风扇旋转色和彩虹模式。
- BLE 命令改为串行发送，增加固件握手、RX 通知、流量异常与物理断线检测；失败时 UI 回退到上次成功档位。
- 液冷未连接时显示明确的泵速/风扇档位占位文字，Mk1 自动禁用不支持的 Mk2 风扇灯效果。
- 修复液冷未连接时“水冷灯光”按钮文字被系统禁用态绘制成黑色；按钮保持白字，点击时提示先连接水冷箱。
- 性能模式、GPU 模式、快捷开关与自定义参数改为状态事件确认，不再依赖 0.5-3.7 秒固定等待；连续切换时只执行最后一次请求，并屏蔽迟到的旧状态回包。
- 模式按钮先即时响应再异步确认，托盘与主界面状态统一由服务层同步，减少重复命令、重复刷新和 UI 线程阻塞。
- MQTT 消息线程移除本地传感器扫描和同步日志写盘；日志改为后台批量写入，LibreHardwareMonitor 仅由悬浮窗的低频采样器读取。
- 键盘 RGB 30 FPS 热路径复用帧缓冲区，消除每帧重复分配；启动阶段的屏幕、自动模式与悬浮窗初始化延后到首屏显示后。
- 修复重复创建 GPU 控制器及多处同步 `Invoke`/无效等待，窗口开启双缓冲；新增快速切换竞态与事件确认测试。
- 修复首次打开自定义模式时短暂出现白色窗口：窗口先完成深色首帧与最终定位再显示，关闭后复用实例以加快再次打开。
- 重做屏幕校色状态机：按官方注册表状态回读、串行切换并等待结果确认，修复随机失败、切换档位后启用勾选消失及重复命令。
- HDR 开启时阻止不兼容的校色操作；禁用遗留 `colors` 开机动作，并自动迁移仍指向旧 beta5.x 的开机任务。

---

## beta5.2（2026-08-15）

- 修复开机电池保护任务在 MQTT 客户端初始化前执行而实际无效的问题，并增加回读确认。
- 与 G-Helper 完全分离退出事件、计划任务和日志目录，避免两款程序互相退出或覆盖任务。
- 所有高风险硬件设置改为按机型能力约束、串行下发和状态回读确认。
- MQTT 改用厂商 ACL 保留槽 `UWPClient_4`（官方 UI 使用 `_5`），并修复重连竞态、退出资源泄漏、RGB 配置字符串解析及 BLE 假成功状态。
- GCU 服务固定安装到 Program Files，安装前验证厂商服务与驱动签名，并阻止端口 13688 的远程入站访问。
- 发布链更新到 `net10.0-windows10.0.19041.0`，默认要求 Authenticode 签名并附带许可证声明。
- 测试迁移到实际发布项目，覆盖风扇安全约束、协议解析、RGB 配置和 MQTT 客户端身份。

---

## beta5.1（2026-08-14）

- 修复：键盘灯效「官方模式 + 睡眠定时器」组合下每 2 秒崩溃（P/Invoke 函数名 GetLastInput → GetLastInputInfo，EntryPointNotFoundException）

---

## beta5（2026-08-14）

### 自定义性能模式完整版（原版 CustomModeSettingPage 全功能移植）
- **4 个自定义档**（自定义 1-4）：切换即生效，每档参数独立存储；主面板「自定义」按钮 + 托盘右键「自定义模式」子菜单均可切换
- **CPU 功耗墙 PL1/PL2**（10-210W）、**CPU 温度墙**（85-100°C，开关+值）、**GPU TGP**（80-150W）、**GPU 动态加速**（开关+5-25）
- **GPU 超频**：超频总开关 + 核心频率偏移（0-300MHz）+ 显存频率偏移（0-1000MHz），经 LCHWOC 通道
- **风扇曲线**：全新专用编辑器（弃用 G-Helper 旧窗口）——16 点占空比曲线、拖动实时保存、按档独立存表；**风扇独立控制开关**（关=双风扇共用 GPU 曲线）
- **Windows 电源计划 + 睿频模式**：每档独立保存与应用（切换档位自动恢复）；电源计划下拉含平衡/高性能/节能/卓越性能，睿频 7 档中文说明
- 恢复默认按钮

### 协议深挖成果（本版关键修正）
- 自定义参数写入：服务端对「同包多字段」只应用部分——改为逐字段发包（与原版 UI 一致）
- TCC/TGP/DB 状态不回推：写后必须 GETSTATUS 主动拉
- TCC 写入语义：目标温度直发直读（85-100°C）
- 风扇曲线：SET_FAN_SPEED_CURVE_SETTING 载荷 T0-T15 全 duty + 表名，逐档存储
- 超频状态：LCHWOC/Control GETSTATUS 请求 Enable/Support

### GCU 独立安装包（配套发布）
- `GCU-独立安装包.zip`：GCUBridge 服务 + GCUService 全依赖 + UWACPIDriver 驱动
- 一键 install.bat（幂等）/ uninstall.bat /「清理官方控制台.bat」（保留 GCU 删其余）
- 支持完整流程：卸载官方控制台 → 装 GCU 中继 → L-Mechrevo 全功能

### 其他
- 托盘右键菜单新增「自定义模式」4 档切换
- 性能面板第 5 列改「自定义」（风扇增强移入快捷开关面板）
- 版本号 beta5

---

## beta4（2026-08-14）

### 液冷系统重做（BLE 直连，不再依赖原控制台）
- **一键自动连接**：点击「未连接（点击连接）」自动扫描附近蓝牙设备（≤12 秒，名称含 LCT 匹配），自动选信号最强并直连；启动时静默自动连接一次。原「连接窗口 / 原控制台回退」全部移除
- **泵速 4 档**：7V / 8V / 11V / 12V（BLE 协议真实电压档；修复原控制台路径下仅 60% 档可用）
- **风扇 5 档**：40 / 50 / 60 / 90 / **100%**（PWM 直写 duty 255；修复 100% 档不转）
- **水冷灯光**：呼吸 / 多彩 / 旋转色 / 彩虹 / 关闭（BLE 灯光帧）
- 移除补水按钮（BLE 协议无此命令）

### 全项目审查修复（97 项发现，三大批次）
**崩溃 / 失效级**
- 删除华硕 ACPIError 启动弹窗（特定机型名可致启动即退出）
- UI 接线幂等化：开机 MQTT 服务未就绪不再出现「已连接但无数据」
- MQTT 订阅失败自动断线重连；删除重复 `Fan/Table` 订阅（双投递）
- 风扇曲线防抖定时器回 UI 线程（修复曲线页刷新静默失效）
- GPU 切换自动重启参数默认改 false（防误触发系统重启）
- 液冷自动连接全链路异常保护（蓝牙关闭/占用不再崩溃）
- Overlay 画刷释放与绘制加锁（修复关闭悬浮窗偶发崩溃）
- 键盘灯效：切换代际原子化 + 串行化（修复并发发帧致 HID 设备崩溃）
- 「停止灯效」不再被睡眠监视器 2 秒内撤销
- GPU 面板初始化时机修复（不再永久隐藏 GPU 按钮）
- 显存百分比按本机实际容量计算（不再硬编码 16GB）

**中危修复**
- SystemEvents / 电源定时器回调封送回 UI 线程；退出停用全部定时器
- 刷新率 / 亮度回显抑制修复（选择不再被 3 秒回读弹回）
- 官方→HID 灯效切换自动补开键盘电源（修复睡眠断电后 HID 灯永不亮）
- 灯效配置全参数校验（手改 cfg 不再崩溃）；灯条/Logo 灯电源开关持久化
- Overlay：桌面/自身前台 FPS 清零、gameOnly alt-tab 隐藏、窗口重建后穿透自愈
- FPS ETW 失败优雅降级；快捷开关并发安全；小键盘锁映射对齐；删除模式切换假等待与误导提示

**死代码清理**
- 删除 AsusService / IntelMsr / ClamshellModeControl；隐藏 Aura / Matrix / FnLock 无效入口；删除启动华硕残留空跑

### Overlay 简洁化
- 移除帧率与曲线图；功耗右移；GPU 功耗跟在 GPU 行后
- **CPU 功耗不显示**（Intel RAPL 需管理员权限，无提权读不到；原版控制台同样不显示）
- GPU 显存显示修复：原「15.9GB」是总显存被误当已用量，现显示实际已用并随负载波动

### 界面
- 快捷开关面板高 DPI 自适应高度，不同分辨率不再截断
- 版本号 beta4

---

## beta3（2026-08-14）

### 新增
- 键盘灯效：新增「开启灯效」按钮，与「停止灯效」并排——停止后可一键恢复当前选中的灯效（官方模式：开灯并重新下发效果；HID 模式：重新进入自定义帧模式并发帧）
- 快捷开关：新增「灯条开关」，可一键开关机箱灯条总电源

### 修复
- 启动不再请求管理员权限（以普通权限运行，overlay 退出与进程结束可直接完成）
- 灯条 / Logo 灯不亮：此前固件电源状态为关闭时调节模式无效，现打开灯效窗口自动开灯，并提供灯光开关
- 灯条 / Logo 灯效无记忆：现使用固件一次性写入（nv_save），重启后保持上次设置
- 官方键盘灯效黑屏：从 HID 模式切到官方固件组时固件可能自动关灯，现切换前自动开灯
- 官方 ↔ HID 模式切换：官方切回 HID 不生效（固件已退出帧模式），现自动重新初始化自定义帧模式
- 官方 ↔ HID 切换时短暂闪现默认彩虹：开灯与下发效果间隔缩短，切换更干净
- 快捷开关在不同分辨率 / DPI 下右侧被裁剪：改为流式自动换行布局
- 深度睡眠开关位置：移至第三行首位，第二行不再截断
- 液冷连接按钮：「未连接（点击连接）」括号被截断，按钮已加宽
- 液冷系统：完成泵速 / 风扇 / 灯光 / 补水全链路底层验证

### 调整
- Logo 灯效精简为 3 种（单色 / 呼吸 / 混合），与原版固件支持列表一致
- 移除小键盘锁开关（硬件不支持，实测无效）

---

## beta2（2026-08-13）

- 首个公开发布版本：三模式（静音 / 平衡 / 狂暴）+ 静音狂暴子模式 + 风扇增强
- 键盘灯效双模式：HID 自定义组（10 种效果）+ 官方固件组（16 种效果）
- 灯条灯效（5 种）+ Logo 灯效
- 液冷系统支持（蓝牙连接 / 泵速 / 风扇 / 灯光 / 补水）
- 悬浮监控（温度 / 频率 / 功耗 / 帧率）
- 显卡三档切换、电池三档、快捷开关、USB 充电、亮度、刷新率、开机自启
- 单文件 exe 发布（ReadyToRun + 单文件内嵌）
